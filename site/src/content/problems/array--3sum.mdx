---
title: "3Sum"
problemSlug: "3sum"
topic: "Array"
topicSlug: "array"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/3sum"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/array/three_sum.py"
tags: ["Array", "Two Pointers", "Sorting"]
patterns: ["Two Pointers", "Sorting", "Deduplication"]
timeComplexity: "O(n²)"
spaceComplexity: "O(1)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["two-sum", "3sum-closest"]
---

## Problem Statement

Given an integer array nums, return all the triplets `[nums[i], nums[j], nums[k]]` such that `i != j`, `i != k`, and `j != k`, and `nums[i] + nums[j] + nums[k] == 0`.

Notice that the solution set must not contain duplicate triplets.

 

**Example 1:**

```
Input: nums = [-1,0,1,2,-1,-4]
Output: [[-1,-1,2],[-1,0,1]]
Explanation: 
nums[0] + nums[1] + nums[2] = (-1) + 0 + 1 = 0.
nums[1] + nums[2] + nums[4] = 0 + 1 + (-1) = 0.
nums[0] + nums[3] + nums[4] = (-1) + 2 + (-1) = 0.
The distinct triplets are [-1,0,1] and [-1,-1,2].
Notice that the order of the output and the order of the triplets does not matter.
```

**Example 2:**

```
Input: nums = [0,1,1]
Output: []
Explanation: The only possible triplet does not sum up to 0.
```

**Example 3:**

```
Input: nums = [0,0,0]
Output: [[0,0,0]]
Explanation: The only possible triplet sums up to 0.
```

 

**Constraints:**

- `3 <= nums.length <= 3000`
- `-10^5 <= nums[i] <= 10^5`

## Solution

```python
from typing import List


class Solution:
    def threeSum(self, nums: List[int]) -> List[List[int]]:
        nums.sort()
        res = []
        for i in range(len(nums)):
            if i > 0 and nums[i] == nums[i - 1]:
                continue
            left, right = i + 1, len(nums) - 1
            while left < right:
                total = nums[i] + nums[left] + nums[right]
                if total < 0:
                    left += 1
                elif total > 0:
                    right -= 1
                else:
                    res.append([nums[i], nums[left], nums[right]])
                    while left < right and nums[left] == nums[left + 1]:
                        left += 1
                    while left < right and nums[right] == nums[right - 1]:
                        right -= 1
                    left += 1
                    right -= 1
        return res
```


## Explanation

### Approach: Sort + Two Pointers

Sort the array first, then for each element, use two pointers to find pairs that sum to the negative of that element. This converts 3Sum into multiple 2Sum problems.

### Why Sorting Enables Two Pointers

After sorting, we can intelligently move two pointers based on whether the current sum is too small (move left pointer right) or too large (move right pointer left). Without sorting, we'd need nested loops checking all pairs.

### Step-by-Step Walkthrough

**Input:** `nums = [-1,0,1,2,-1,-4]`

```
Step 1: Sort → [-4,-1,-1,0,1,2]

Step 2: For each element, find two elements that sum to its negative

i=0 (num=-4):
  Need two elements summing to 4
  left=1, right=5: (-1) + 2 = 1 (too small, move left++)
  left=2, right=5: (-1) + 2 = 1 (too small, move left++)
  left=3, right=5: 0 + 2 = 2 (too small, move left++)
  left=4, right=5: 1 + 2 = 3 (still too small, left >= right, break)

i=1 (num=-1):
  Need two elements summing to 1
  left=2, right=5: (-1) + 2 = 1 ✓ FOUND: [-1,-1,2]
  Skip duplicates, continue...
  left=3, right=4: 0 + 1 = 1 ✓ FOUND: [-1,0,1]

Continue for remaining elements...
```

**Result:** `[[-1,-1,2], [-1,0,1]]`

### Code Breakdown

```python
nums.sort()
res = []
for i in range(len(nums) - 2):
    if i > 0 and nums[i] == nums[i-1]:
        continue  # Skip duplicates

    left, right = i + 1, len(nums) - 1
    while left < right:
        total = nums[i] + nums[left] + nums[right]
        if total < 0:
            left += 1  # Need larger sum
        elif total > 0:
            right -= 1  # Need smaller sum
        else:
            res.append([nums[i], nums[left], nums[right]])
            # Skip duplicates
            while left < right and nums[left] == nums[left+1]:
                left += 1
            while left < right and nums[right] == nums[right-1]:
                right -= 1
            left += 1
            right -= 1

return res
```

### Why O(n²) is Optimal for 3Sum

Outer loop: O(n), Inner two-pointer: O(n), Total: O(n²). Since we must consider all elements and their pairings, O(n²) is the theoretical minimum.


## Complexity

- **Time**: `O(n²)`
- **Space**: `O(1)`


## Similar Problems

- [Two Sum](/TechInterviewHandbookDSA169/problems/two-sum)
- [3Sum Closest](/TechInterviewHandbookDSA169/problems/3sum-closest)
