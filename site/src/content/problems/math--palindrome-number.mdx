---
title: "Palindrome Number"
problemSlug: "palindrome-number"
topic: "Math"
topicSlug: "math"
difficulty: "Easy"
leetcodeUrl: "https://leetcode.com/problems/palindrome-number"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/math/palindrome_number.py"
tags: ["Math"]
patterns: ["Digit Reversal", "No String Conversion"]
timeComplexity: "O(log n)"
spaceComplexity: "O(1)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["reverse-integer"]
---

## Problem Statement

Given an integer `x`, return `true`_ if _`x`_ is a __**palindrome**__, and _`false`_ otherwise_.

 

**Example 1:**

```
Input: x = 121
Output: true
Explanation: 121 reads as 121 from left to right and from right to left.
```

**Example 2:**

```
Input: x = -121
Output: false
Explanation: From left to right, it reads -121. From right to left, it becomes 121-. Therefore it is not a palindrome.
```

**Example 3:**

```
Input: x = 10
Output: false
Explanation: Reads 01 from right to left. Therefore it is not a palindrome.
```

 

**Constraints:**

- `-2^31 <= x <= 2^31 - 1`

 

**Follow up:** Could you solve it without converting the integer to a string?

## Solution

```python
class Solution:
    def isPalindrome(self, x: int) -> bool:
        if x < 0:
            return False
        return x == int(str(x)[::-1])
```


## Explanation

### Approach: Digit Reversal

This solution reverses digits one by one and rebuilds the result while preserving sign constraints.

### Intuition

Negative numbers cannot be palindromes because of the leading minus sign. For non-negative numbers, the simplest way to test symmetry is to convert the value to a string and compare it with its reversed version.

### Algorithm

Here's the step-by-step approach:

1. If `x` is negative, return `False` immediately.
2. Convert `x` to a string.
3. Reverse that string.
4. Return whether the original and reversed strings are equal.

### Code Breakdown

The implementation uses Python slicing `[::-1]` to build the reversed digit string in one expression, then converts it back to an integer for comparison with the original number.

### Why This Works

- **Palindrome definition:** A number is a palindrome exactly when its digit sequence reads the same forward and backward.
- **Negative-number exclusion:** The minus sign makes any negative value asymmetric.
- **Direct symmetry check:** Reversing the digit string gives a straightforward test of that forward/backward equality.


## Complexity

- **Time**: `O(log n)`
- **Space**: `O(1)`


## Similar Problems

- [Reverse Integer](/TechInterviewHandbookDSA169/problems/reverse-integer)
