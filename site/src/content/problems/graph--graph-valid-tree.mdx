---
title: "Graph Valid Tree"
problemSlug: "graph-valid-tree"
topic: "Graph"
topicSlug: "graph"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/graph-valid-tree"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/graph/graph_valid_tree.py"
tags: ["Depth-First Search", "Breadth-First Search", "Union Find", "Graph"]
patterns: ["Union Find", "DFS", "Cycle Detection"]
timeComplexity: "O(V+E)"
spaceComplexity: "O(V)"
hasProblemStatement: true
hasConstraints: false
hasExamples: true
isPremium: true
similarProblems: ["accounts-merge", "number-of-connected-components-in-an-undirected-graph"]
---

## Problem Statement

You are given a graph represented as an integer n (the number of nodes, labeled from 0 to n-1) and a list of edges
where each edge is a pair of nodes [u, v] that connects two nodes in the graph.

Write a function to determine if the graph is a valid tree.

A valid tree must satisfy the following conditions:

It is connected (there is a path between any two nodes).
It contains no cycles.


## Examples

Example 1:

Example 1:
Input: n = 5, edges = [[0,1],[0,2],[0,3],[1,4]]
Output: true

Example 2:

Example 2:
Input: n = 5, edges = [[0,1],[1,2],[2,3],[1,3],[1,4]]
Output: false


## Solution

```python
from typing import List


class Solution:
    def validTree(self, n: int, edges: List[List[int]]) -> bool:
        if n - 1 != len(edges):
            return False
        parent = list(range(n))
        def find(x):
            if parent[x] != x:
                parent[x] = find(parent[x])
            return parent[x]
        def union(x, y):
            root_x, root_y = find(x), find(y)
            if root_x == root_y:
                return False
            parent[root_x] = root_y
            return True
        for u, v in edges:
            if not union(u, v):
                return False
        return True
```


## Explanation

### Approach

This solution uses **Depth-First Search (DFS)** to explore all possible paths.

### Intuition

The solution breaks down the problem into manageable steps, processing elements systematically to build the final answer.

### Algorithm

1. **Handle edge cases**: Check for empty or invalid input
2. **Return result**: Output the computed answer

### Why This Works

- **Systematic approach**: Processes elements in a logical order to build the solution
- **Correctness**: The algorithm handles all edge cases and produces the expected output
- **Efficiency**: Optimized for both time and space constraints


## Complexity

- **Time**: `O(V+E)`
- **Space**: `O(V)`


## Similar Problems

- [Accounts Merge](/TechInterviewHandbookDSA169/problems/accounts-merge)
- [Number Of Connected Components In An Undirected Graph](/TechInterviewHandbookDSA169/problems/number-of-connected-components-in-an-undirected-graph)
