---
title: "Longest Valid Parentheses"
problemSlug: "longest-valid-parentheses"
topic: "Stack"
topicSlug: "stack"
difficulty: "Hard"
leetcodeUrl: "https://leetcode.com/problems/longest-valid-parentheses"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/stack/longest_valid_parentheses.py"
tags: ["String", "Dynamic Programming", "Stack"]
patterns: ["Stack", "Dynamic Programming"]
timeComplexity: "O(n)"
spaceComplexity: "O(n)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["valid-parentheses"]
---

## Problem Statement

Given a string containing just the characters `'('` and `')'`, return _the length of the longest valid (well-formed) parentheses __substring_.

 

**Example 1:**

```
Input: s = "(()"
Output: 2
Explanation: The longest valid parentheses substring is "()".
```

**Example 2:**

```
Input: s = ")()())"
Output: 4
Explanation: The longest valid parentheses substring is "()()".
```

**Example 3:**

```
Input: s = ""
Output: 0
```

 

**Constraints:**

- `0 <= s.length <= 3 * 10^4`
- `s[i]` is `'('`, or `')'`.

## Solution

```python
class Solution:
    def longestValidParentheses(self, s: str) -> int:
        stack = [-1]
        max_len = 0
        for i, c in enumerate(s):
            if c == '(':
                stack.append(i)
            else:
                stack.pop()
                if stack:
                    max_len = max(max_len, i - stack[-1])
                else:
                    stack.append(i)
        return max_len
```


## Explanation

### Approach: Stack of Indices

This solution uses a **stack of indices** to track unmatched parentheses and measure the length of valid segments.

### Intuition

The stack stores the most recent unmatched position. When we see `'('`, we push its index. When we see `')'`, we pop. If the stack becomes empty, the current `')'` becomes the new boundary; otherwise, the distance from the current index to the new stack top is the length of a valid suffix.

### Algorithm

Here's the step-by-step approach:

1. Initialize the stack with `-1` as a sentinel boundary.
2. Traverse the string character by character.
3. Push indices of `'('`; for `')'`, pop once. If the stack becomes empty, push the current index as the new boundary.
4. Otherwise, update the best answer with `i - stack[-1]`.

### Code Breakdown

The sentinel `-1` lets valid substrings starting at index `0` compute their lengths correctly. Every unmatched closing parenthesis resets the base boundary for future valid substrings.

### Why This Works

- **Boundary tracking:** The stack top always marks the index before the current valid suffix begins.
- **Correct matching behavior:** Pushing `'('` indices and popping on `')'` mirrors parenthesis matching directly.
- **Maximum extraction:** Every time a valid suffix ends at `i`, `i - stack[-1]` gives its exact length.


## Complexity

- **Time**: `O(n)`
- **Space**: `O(n)`


## Similar Problems

- [Valid Parentheses](/TechInterviewHandbookDSA169/problems/valid-parentheses)
