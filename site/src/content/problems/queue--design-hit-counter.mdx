---
title: "Design Hit Counter"
problemSlug: "design-hit-counter"
topic: "Queue"
topicSlug: "queue"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/design-hit-counter"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/queue/design_hit_counter.py"
tags: ["Array", "Binary Search", "Design", "Queue", "Data Stream"]
patterns: ["Circular Buffer", "Binary Search", "Sliding Window"]
timeComplexity: "O(1) hit, O(n) getHits"
spaceComplexity: "O(k)"
hasProblemStatement: true
hasConstraints: false
hasExamples: true
isPremium: true
similarProblems: []
---

## Problem Statement

Design a hit counter which counts the number of hits received in the past 5 minutes.

Each function accepts a timestamp parameter (in seconds granularity) and you may assume that calls are being made to
the system in chronological order (i.e., timestamp is monotonically increasing). You may assume that the earliest
timestamp starts at 1.

It is possible that several hits arrive roughly at the same time.

Implement the HitCounter class:

HitCounter() Initializes the object of the hit counter system.

void hit(int timestamp) Records a hit that happened at the given timestamp.

int getHits(int timestamp) Returns the number of hits in the past 5 minutes from the given timestamp.


## Examples

Example 1:

Example 1:

Input
["HitCounter", "hit", "hit", "hit", "getHits", "hit", "getHits"]
[[], [1], [2], [3], [4], [300], [300]]
Output
[null, null, null, null, 3, null, 4]


## Solution

```python
from collections import deque


class HitCounter:
    def __init__(self):
        self.hits = deque()

    def hit(self, timestamp: int) -> None:
        if not self.hits or self.hits[-1][0] != timestamp:
            self.hits.append([timestamp, 1])
        else:
            self.hits[-1][1] += 1

    def getHits(self, timestamp: int) -> int:
        while self.hits and timestamp - self.hits[0][0] >= 300:
            self.hits.popleft()
        return sum(hit[1] for hit in self.hits)
```


## Explanation

### Approach: Sliding Time Window with Compressed Deque

Store hits as `(timestamp, count)` in a deque.

- `hit(t)`: if latest timestamp equals `t`, increment its count; otherwise append new bucket
- `getHits(t)`: remove buckets older than 300 seconds, then sum counts in remaining buckets

This keeps only relevant recent data.

### Step-by-Step Example

Operations:

- `hit(1), hit(2), hit(3)` -> deque `[(1,1),(2,1),(3,1)]`
- `getHits(4)` -> none expired -> total `3`
- `hit(300)` -> deque `[(1,1),(2,1),(3,1),(300,1)]`
- `getHits(300)` -> timestamp `1` still valid (`300-1 < 300`) -> total `4`

At `getHits(301)`, bucket `1` expires (`301-1 >= 300`) and is removed.

### Code Breakdown

```python
while self.hits and timestamp - self.hits[0][0] >= 300:
    self.hits.popleft()
return sum(hit[1] for hit in self.hits)
```

### Why This Works

- Deque is ordered by time (timestamps are monotonic)
- Expired hits are always at front, so cleanup is efficient
- Compression by timestamp avoids storing every individual hit


## Complexity

- **Time**: `O(1) hit, O(n) getHits`
- **Space**: `O(k)` where `k` is number of timestamp buckets in last 300 seconds
