---
title: "01 Matrix"
problemSlug: "01-matrix"
topic: "Graph"
topicSlug: "graph"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/01-matrix"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/graph/01_matrix.py"
tags: ["Array", "Dynamic Programming", "Breadth-First Search", "Matrix"]
patterns: ["BFS Multi-source", "Dynamic Programming"]
timeComplexity: "O(m×n)"
spaceComplexity: "O(m×n)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["flood-fill", "rotting-oranges"]
---

## Problem Statement

Given an `m x n` binary matrix `mat`, return _the distance of the nearest _`0`_ for each cell_.

The distance between two cells sharing a common edge is `1`.

 

**Example 1:**

```
Input: mat = [[0,0,0],[0,1,0],[0,0,0]]
Output: [[0,0,0],[0,1,0],[0,0,0]]
```

**Example 2:**

```
Input: mat = [[0,0,0],[0,1,0],[1,1,1]]
Output: [[0,0,0],[0,1,0],[1,2,1]]
```

 

**Constraints:**

- `m == mat.length`
- `n == mat[i].length`
- `1 <= m, n <= 10^4`
- `1 <= m * n <= 10^4`
- `mat[i][j]` is either `0` or `1`.
- There is at least one `0` in `mat`.

 

**Note:** This question is the same as 1765: https://leetcode.com/problems/map-of-highest-peak/

## Solution

```python
from collections import deque
from typing import List


class Solution:
    def updateMatrix(self, mat: List[List[int]]) -> List[List[int]]:
        rows, cols = len(mat), len(mat[0])
        queue = deque()
        for i in range(rows):
            for j in range(cols):
                if mat[i][j] == 0:
                    queue.append((i, j))
                else:
                    mat[i][j] = float('inf')
        directions = [(0, 1), (0, -1), (1, 0), (-1, 0)]
        while queue:
            cell = queue.popleft()
            for direction in directions:
                i, j = cell[0] + direction[0], cell[1] + direction[1]
                if i < 0 or j < 0 or i >= rows or j >= cols or mat[i][j] <= mat[cell[0]][cell[1]] + 1:
                    continue
                queue.append((i, j))
                mat[i][j] = mat[cell[0]][cell[1]] + 1
        return mat
```


## Explanation

### Approach: Multi-Source BFS

This solution starts BFS from **all zero cells at once**, letting distances spread outward to the nearest ones.

### Intuition

Every `0` cell is already at distance `0` from the nearest zero, so they can all be treated as simultaneous BFS sources. As the search expands level by level, the first time we improve a `1` cell is guaranteed to be through the shortest path to some zero.

### Algorithm

Here's the step-by-step approach:

1. Traverse the matrix and enqueue every cell containing `0`.
2. Set every `1` cell to `infinity` so it can later be relaxed to its true shortest distance.
3. Run BFS from the queue, and whenever a neighbor can be improved to `current + 1`, update it and enqueue it.
4. Return the modified matrix once all reachable cells have been assigned their minimum distance.

### Code Breakdown

The matrix itself is reused as the distance table. Initial zero cells seed the queue, and each BFS step tries the four adjacent directions, updating a neighbor only when the new distance is smaller than its current value.

### Why This Works

- **Shortest-path layering:** BFS explores cells in nondecreasing distance order.
- **All zeros as sources:** Starting from every zero at once guarantees each `1` cell is reached from its nearest zero first.
- **Monotonic updates:** Once a cell gets its minimum distance, no later path can improve it with fewer steps.


## Complexity

- **Time**: `O(m×n)`
- **Space**: `O(m×n)`


## Similar Problems

- [Flood Fill](/TechInterviewHandbookDSA169/problems/flood-fill)
- [Rotting Oranges](/TechInterviewHandbookDSA169/problems/rotting-oranges)
