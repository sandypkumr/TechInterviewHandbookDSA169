---
title: "Trapping Rain Water"
problemSlug: "trapping-rain-water"
topic: "Stack"
topicSlug: "stack"
difficulty: "Hard"
leetcodeUrl: "https://leetcode.com/problems/trapping-rain-water"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/stack/trapping_rain_water.py"
tags: ["Array", "Two Pointers", "Dynamic Programming", "Stack", "Monotonic Stack"]
patterns: ["Two Pointers", "Prefix Max Arrays", "Monotonic Stack"]
timeComplexity: "O(n)"
spaceComplexity: "O(1)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["container-with-most-water", "largest-rectangle-in-histogram"]
---

## Problem Statement

Given `n` non-negative integers representing an elevation map where the width of each bar is `1`, compute how much water it can trap after raining.

 

**Example 1:**

```
Input: height = [0,1,0,2,1,0,1,3,2,1,2,1]
Output: 6
Explanation: The above elevation map (black section) is represented by array [0,1,0,2,1,0,1,3,2,1,2,1]. In this case, 6 units of rain water (blue section) are being trapped.
```

**Example 2:**

```
Input: height = [4,2,0,3,2,5]
Output: 9
```

 

**Constraints:**

- `n == height.length`
- `1 <= n <= 2 * 10^4`
- `0 <= height[i] <= 10^5`

## Solution

```python
from typing import List


class Solution:
    def trap(self, height: List[int]) -> int:
        stack = []
        water = 0
        for i, h in enumerate(height):
            while stack and h > height[stack[-1]]:
                top = stack.pop()
                if not stack:
                    break
                distance = i - stack[-1] - 1
                water += distance * (min(h, height[stack[-1]]) - height[top])
            stack.append(i)
        return water
```


## Explanation

### Approach

This solution implements an efficient algorithm to solve the problem systematically.

### Intuition

The solution breaks down the problem into manageable steps, processing elements systematically to build the final answer.

### Algorithm

1. **Main loop**: Continue until condition is met
2. **Return result**: Output the computed answer

### Why This Works

- **Systematic approach**: Processes elements in a logical order to build the solution
- **Correctness**: The algorithm handles all edge cases and produces the expected output
- **Efficiency**: Optimized for both time and space constraints


## Complexity

- **Time**: `O(n)`
- **Space**: `O(1)`


## Similar Problems

- [Container With Most Water](/TechInterviewHandbookDSA169/problems/container-with-most-water)
- [Largest Rectangle In Histogram](/TechInterviewHandbookDSA169/problems/largest-rectangle-in-histogram)
