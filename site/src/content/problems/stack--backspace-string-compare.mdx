---
title: "Backspace String Compare"
problemSlug: "backspace-string-compare"
topic: "Stack"
topicSlug: "stack"
difficulty: "Easy"
leetcodeUrl: "https://leetcode.com/problems/backspace-string-compare"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/stack/backspace_string_compare.py"
tags: ["Two Pointers", "String", "Stack", "Simulation"]
patterns: ["Stack Simulation", "Two Pointers"]
timeComplexity: "O(n)"
spaceComplexity: "O(1)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["valid-parentheses"]
---

## Problem Statement

Given two strings `s` and `t`, return `true` _if they are equal when both are typed into empty text editors_. `'#'` means a backspace character.

Note that after backspacing an empty text, the text will continue empty.

 

**Example 1:**

```
Input: s = "ab#c", t = "ad#c"
Output: true
Explanation: Both s and t become "ac".
```

**Example 2:**

```
Input: s = "ab##", t = "c#d#"
Output: true
Explanation: Both s and t become "".
```

**Example 3:**

```
Input: s = "a#c", t = "b"
Output: false
Explanation: s becomes "c" while t becomes "b".
```

 

**Constraints:**

- `1 <= s.length, t.length <= 200`
- `s` and `t` only contain lowercase letters and `'#'` characters.

 

**Follow up:** Can you solve it in `O(n)` time and `O(1)` space?

## Solution

```python
class Solution:
    def backspaceCompare(self, s: str, t: str) -> bool:
        def build(s: str) -> str:
            stack = []
            for c in s:
                if c == '#':
                    if stack:
                        stack.pop()
                else:
                    stack.append(c)
            return ''.join(stack)
        return build(s) == build(t)
```


## Complexity

- **Time**: `O(n)`
- **Space**: `O(1)`


## Similar Problems

- [Valid Parentheses](/TechInterviewHandbookDSA169/problems/valid-parentheses)
