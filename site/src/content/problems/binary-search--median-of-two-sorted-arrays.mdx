---
title: "Median of Two Sorted Arrays"
problemSlug: "median-of-two-sorted-arrays"
topic: "Binary Search"
topicSlug: "binary-search"
difficulty: "Hard"
leetcodeUrl: "https://leetcode.com/problems/median-of-two-sorted-arrays"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/binary_search/median_of_two_sorted_arrays.py"
tags: ["Array", "Binary Search", "Divide and Conquer"]
patterns: ["Binary Search", "Partition"]
timeComplexity: "O(log(m+n))"
spaceComplexity: "O(1)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["find-k-closest-elements"]
---

## Problem Statement

Given two sorted arrays `nums1` and `nums2` of size `m` and `n` respectively, return **the median** of the two sorted arrays.

The overall run time complexity should be `O(log (m+n))`.

 

**Example 1:**

```
Input: nums1 = [1,3], nums2 = [2]
Output: 2.00000
Explanation: merged array = [1,2,3] and median is 2.
```

**Example 2:**

```
Input: nums1 = [1,2], nums2 = [3,4]
Output: 2.50000
Explanation: merged array = [1,2,3,4] and median is (2 + 3) / 2 = 2.5.
```

 

**Constraints:**

- `nums1.length == m`
- `nums2.length == n`
- `0 <= m <= 1000`
- `0 <= n <= 1000`
- `1 <= m + n <= 2000`
- `-10^6 <= nums1[i], nums2[i] <= 10^6`

## Solution

```python
from typing import List


class Solution:
    def findMedianSortedArrays(self, nums1: List[int], nums2: List[int]) -> float:
        if len(nums1) > len(nums2):
            nums1, nums2 = nums2, nums1
        x, y = len(nums1), len(nums2)
        left, right = 0, x
        while left <= right:
            partition_x = (left + right) // 2
            partition_y = (x + y + 1) // 2 - partition_x
            max_left_x = float('-inf') if partition_x == 0 else nums1[partition_x - 1]
            min_right_x = float('inf') if partition_x == x else nums1[partition_x]
            max_left_y = float('-inf') if partition_y == 0 else nums2[partition_y - 1]
            min_right_y = float('inf') if partition_y == y else nums2[partition_y]
            if max_left_x <= min_right_y and max_left_y <= min_right_x:
                if (x + y) % 2 == 0:
                    return (max(max_left_x, max_left_y) + min(min_right_x, min_right_y)) / 2
                else:
                    return max(max_left_x, max_left_y)
            elif max_left_x > min_right_y:
                right = partition_x - 1
            else:
                left = partition_x + 1
        return -1
```


## Explanation

### Approach: Binary Search on Partitions

This solution performs **binary search** on the smaller array to find a partition where the combined left half and right half are correctly ordered.

### Intuition

The median splits the combined sorted order into a left half and a right half. Instead of merging the arrays, we search for a cut in `nums1` and infer the matching cut in `nums2` so that every value on the left is less than or equal to every value on the right.

### Algorithm

Here's the step-by-step approach:

1. Ensure `nums1` is the smaller array so the binary search range stays minimal.
2. Binary search a partition index `partition_x` in `nums1`, then compute the matching `partition_y` in `nums2` so the left half contains half the elements.
3. Check whether `max_left_x <= min_right_y` and `max_left_y <= min_right_x`. If not, move the partition left or right.
4. Once the partition is valid, return either the middle element or the average of the two middle elements depending on total parity.

### Code Breakdown

The code uses sentinel values (`-inf` and `inf`) so partitions at the ends of an array can be handled without special-case branching. Each loop iteration checks whether the partition is valid and shrinks the search range accordingly.

### Why This Works

- **Correct partition condition:** When both left-side maximums are no larger than the opposite right-side minimums, the two halves are properly ordered.
- **Binary search direction:** If `max_left_x > min_right_y`, the cut in `nums1` is too far right; otherwise it is too far left.
- **Median from boundaries:** Once the partition is valid, the median depends only on the boundary values around the cut, not on the full merged array.


## Complexity

- **Time**: `O(log(m+n))`
- **Space**: `O(1)`


## Similar Problems

- [Find K Closest Elements](/TechInterviewHandbookDSA169/problems/find-k-closest-elements)
