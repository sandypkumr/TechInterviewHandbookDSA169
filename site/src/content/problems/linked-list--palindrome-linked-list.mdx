---
title: "Palindrome Linked List"
problemSlug: "palindrome-linked-list"
topic: "Linked List"
topicSlug: "linked-list"
difficulty: "Easy"
leetcodeUrl: "https://leetcode.com/problems/palindrome-linked-list"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/linked_list/palindrome_linked_list.py"
tags: ["Linked List", "Two Pointers", "Stack", "Recursion"]
patterns: ["Fast and Slow Pointers", "Reverse Second Half"]
timeComplexity: "O(n)"
spaceComplexity: "O(1)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["reverse-linked-list", "middle-of-the-linked-list"]
---

## Problem Statement

Given the `head` of a singly linked list, return `true`_ if it is a __palindrome__ or _`false`_ otherwise_.

 

**Example 1:**

```
Input: head = [1,2,2,1]
Output: true
```

**Example 2:**

```
Input: head = [1,2]
Output: false
```

 

**Constraints:**

- The number of nodes in the list is in the range `[1, 10^5]`.
- `0 <= Node.val <= 9`

 

**Follow up:** Could you do it in `O(n)` time and `O(1)` space?

## Solution

```python
from typing import Optional


class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next


class Solution:
    def isPalindrome(self, head: Optional[ListNode]) -> bool:
        slow = fast = head
        while fast and fast.next:
            slow = slow.next
            fast = fast.next.next
        prev = None
        while slow:
            current = slow
            slow = slow.next
            current.next = prev
            prev = current
        while prev:
            if head.val != prev.val:
                return False
            head = head.next
            prev = prev.next
        return True
```


## Explanation

### Approach: Find Middle, Reverse Second Half, Compare

This solution checks palindrome in O(1) extra space by using three phases:
1. Find the middle with fast/slow pointers
2. Reverse the second half in-place
3. Compare first half and reversed second half node-by-node

### Step-by-Step Idea

For `1 -> 2 -> 2 -> 1`:

- Middle search ends `slow` at 3rd node (`2`)
- Reverse from `slow`: `2 -> 1` becomes `1 -> 2`
- Compare from start and reversed head:
  - `1 == 1`
  - `2 == 2`
- All matched -> palindrome

For odd length, middle naturally gets included in second-half reversal, and comparison still works because we only compare while `prev` exists.

### Code Breakdown

```python
slow = fast = head
while fast and fast.next:
    slow = slow.next
    fast = fast.next.next

prev = None
while slow:
    current = slow
    slow = slow.next
    current.next = prev
    prev = current

while prev:
    if head.val != prev.val:
        return False
    head = head.next
    prev = prev.next
```

### Why This Works

- Fast/slow splits list at midpoint in one pass
- Reversing second half lets us compare symmetric positions linearly
- Each node is visited O(1) times overall -> O(n) time, O(1) extra space


## Complexity

- **Time**: `O(n)`
- **Space**: `O(1)`


## Similar Problems

- [Reverse Linked List](/TechInterviewHandbookDSA169/problems/reverse-linked-list)
- [Middle Of The Linked List](/TechInterviewHandbookDSA169/problems/middle-of-the-linked-list)
