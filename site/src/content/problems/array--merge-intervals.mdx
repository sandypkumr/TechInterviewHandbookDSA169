---
title: "Merge Intervals"
problemSlug: "merge-intervals"
topic: "Array"
topicSlug: "array"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/merge-intervals"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/array/merge_intervals.py"
tags: ["Array", "Sorting"]
patterns: ["Sorting", "Interval Merging", "Sweep Line"]
timeComplexity: "O(n log n)"
spaceComplexity: "O(n)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["insert-interval", "meeting-rooms-ii", "non-overlapping-intervals"]
---

## Problem Statement

Given an array of `intervals` where `intervals[i] = [start_i, end_i]`, merge all overlapping intervals, and return _an array of the non-overlapping intervals that cover all the intervals in the input_.

 

**Example 1:**

```
Input: intervals = [[1,3],[2,6],[8,10],[15,18]]
Output: [[1,6],[8,10],[15,18]]
Explanation: Since intervals [1,3] and [2,6] overlap, merge them into [1,6].
```

**Example 2:**

```
Input: intervals = [[1,4],[4,5]]
Output: [[1,5]]
Explanation: Intervals [1,4] and [4,5] are considered overlapping.
```

**Example 3:**

```
Input: intervals = [[4,7],[1,4]]
Output: [[1,7]]
Explanation: Intervals [1,4] and [4,7] are considered overlapping.
```

 

**Constraints:**

- `1 <= intervals.length <= 10^4`
- `intervals[i].length == 2`
- `0 <= start_i <= end_i <= 10^4`

## Solution

```python
from typing import List


class Solution:
    def merge(self, intervals: List[List[int]]) -> List[List[int]]:
        intervals.sort(key=lambda x: x[0])
        res = []
        for interval in intervals:
            if not res or interval[0] > res[-1][1]:
                res.append(interval)
            else:
                res[-1][1] = max(res[-1][1], interval[1])
        return res
```


## Explanation

### Approach: Sort and Merge

**Sort intervals by start time**, then iterate through and merge overlapping ones. The key insight: after sorting, overlapping intervals are consecutive, making merging straightforward.

### Why Sorting is Key

Before sorting, identifying overlaps requires checking all pairs. After sorting by start time, we only need to check consecutive intervals.

### The Merge Logic

For two intervals `[a, b]` and `[c, d]` (where `a ≤ c` after sorting):
- **Overlapping:** if `c ≤ b`, they overlap → merge to `[a, max(b,d)]`
- **Not overlapping:** if `c > b`, they don't overlap → keep both

### Step-by-Step Example

**Input:** `intervals = [[1,3],[2,6],[8,10],[15,18]]`

```
Step 1: Sort by start time
Result: [[1,3],[2,6],[8,10],[15,18]]  (already sorted)

Step 2: Merge
- current = [1,3]
- See [2,6]: 2 ≤ 3? YES, overlaps! → Merge to [1,6]
- See [8,10]: 8 ≤ 6? NO, doesn't overlap → Add [1,6] to result, current = [8,10]
- See [15,18]: 15 ≤ 10? NO, doesn't overlap → Add [8,10] to result, current = [15,18]
- End: Add [15,18] to result

Result: [[1,6],[8,10],[15,18]] ✓
```

### Code

```python
intervals.sort()  # Sort by start time (Python sorts tuples by first element)
merged = []
for start, end in intervals:
    if merged and start <= merged[-1][1]:
        # Overlaps with last interval - merge
        merged[-1][1] = max(merged[-1][1], end)
    else:
        # No overlap - add new interval
        merged.append([start, end])
return merged
```

### Complexity Analysis

- **Time:** O(n log n) - sorting dominates
- **Space:** O(n) - for output (or O(1) if we sort in-place and modify input)


## Complexity

- **Time**: `O(n log n)`
- **Space**: `O(n)`


## Similar Problems

- [Insert Interval](/TechInterviewHandbookDSA169/problems/insert-interval)
- [Meeting Rooms Ii](/TechInterviewHandbookDSA169/problems/meeting-rooms-ii)
- [Non Overlapping Intervals](/TechInterviewHandbookDSA169/problems/non-overlapping-intervals)
