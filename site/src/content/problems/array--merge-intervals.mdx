---
title: "Merge Intervals"
problemSlug: "merge-intervals"
topic: "Array"
topicSlug: "array"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/merge-intervals"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/array/merge_intervals.py"
tags: ["Array", "Sorting"]
patterns: ["Sorting", "Interval Merging", "Sweep Line"]
timeComplexity: "O(n log n)"
spaceComplexity: "O(n)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["insert-interval", "meeting-rooms-ii", "non-overlapping-intervals"]
---

## Problem Statement

Given an array of `intervals` where `intervals[i] = [start_i, end_i]`, merge all overlapping intervals, and return _an array of the non-overlapping intervals that cover all the intervals in the input_.

 

**Example 1:**

```
Input: intervals = [[1,3],[2,6],[8,10],[15,18]]
Output: [[1,6],[8,10],[15,18]]
Explanation: Since intervals [1,3] and [2,6] overlap, merge them into [1,6].
```

**Example 2:**

```
Input: intervals = [[1,4],[4,5]]
Output: [[1,5]]
Explanation: Intervals [1,4] and [4,5] are considered overlapping.
```

**Example 3:**

```
Input: intervals = [[4,7],[1,4]]
Output: [[1,7]]
Explanation: Intervals [1,4] and [4,7] are considered overlapping.
```

 

**Constraints:**

- `1 <= intervals.length <= 10^4`
- `intervals[i].length == 2`
- `0 <= start_i <= end_i <= 10^4`

## Solution

```python
from typing import List


class Solution:
    def merge(self, intervals: List[List[int]]) -> List[List[int]]:
        intervals.sort(key=lambda x: x[0])
        res = []
        for interval in intervals:
            if not res or interval[0] > res[-1][1]:
                res.append(interval)
            else:
                res[-1][1] = max(res[-1][1], interval[1])
        return res
```


## Complexity

- **Time**: `O(n log n)`
- **Space**: `O(n)`


## Similar Problems

- [Insert Interval](/TechInterviewHandbookDSA169/problems/insert-interval)
- [Meeting Rooms Ii](/TechInterviewHandbookDSA169/problems/meeting-rooms-ii)
- [Non Overlapping Intervals](/TechInterviewHandbookDSA169/problems/non-overlapping-intervals)
