---
title: "Rotate Array"
problemSlug: "rotate-array"
topic: "Array"
topicSlug: "array"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/rotate-array"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/array/rotate_array.py"
tags: ["Array", "Two Pointers", "Math"]
patterns: ["Reversal Algorithm", "Cyclic Replacement"]
timeComplexity: "O(n)"
spaceComplexity: "O(n)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: []
---

## Problem Statement

Given an integer array `nums`, rotate the array to the right by `k` steps, where `k` is non-negative.

 

**Example 1:**

```
Input: nums = [1,2,3,4,5,6,7], k = 3
Output: [5,6,7,1,2,3,4]
Explanation:
rotate 1 steps to the right: [7,1,2,3,4,5,6]
rotate 2 steps to the right: [6,7,1,2,3,4,5]
rotate 3 steps to the right: [5,6,7,1,2,3,4]
```

**Example 2:**

```
Input: nums = [-1,-100,3,99], k = 2
Output: [3,99,-1,-100]
Explanation: 
rotate 1 steps to the right: [99,-1,-100,3]
rotate 2 steps to the right: [3,99,-1,-100]
```

 

**Constraints:**

- `1 <= nums.length <= 10^5`
- `-2^31 <= nums[i] <= 2^31 - 1`
- `0 <= k <= 10^5`

 

**Follow up:**

- Try to come up with as many solutions as you can. There are at least **three** different ways to solve this problem.
- Could you do it in-place with `O(1)` extra space?

## Solution

```python
from typing import List


class Solution:
    def rotate(self, nums: List[int], k: int) -> None:
        """
        Do not return anything, modify nums in-place instead.
        """
        n = len(nums)
        k %= n
        nums[:] = nums[n - k:] + nums[:n - k]
```


## Explanation

### Approach: Slice Split and Rebuild

This implementation rotates by splitting the array into two parts and concatenating:

- right part: last `k` elements
- left part: first `n-k` elements

Then assign back in-place via `nums[:] = ...`.

### Why `k %= n` is Needed

If `k` is larger than array size, rotating `n` times returns the same array. So only `k % n` effective rotations matter.

### Step-by-Step Walkthrough

**Input:** `nums = [1,2,3,4,5,6,7], k = 3`

- `n = 7`, `k = 3`
- `nums[n-k:] = nums[4:] = [5,6,7]`
- `nums[:n-k] = nums[:4] = [1,2,3,4]`
- Concatenate -> `[5,6,7,1,2,3,4]`

Assign back:

```python
nums[:] = nums[n-k:] + nums[:n-k]
```

Using `nums[:]` modifies the same list object (important in LeetCode-style in-place APIs).

### Code Breakdown

```python
n = len(nums)
k %= n
nums[:] = nums[n - k:] + nums[:n - k]
```

Compact and readable solution.

### Note on Follow-up

This specific implementation uses slicing and concatenation (very Pythonic). A strict O(1) extra-space follow-up is usually solved with the three-reversal method.


## Complexity

- **Time**: `O(n)`
- **Space**: `O(n)`
