---
title: "Combination Sum"
problemSlug: "combination-sum"
topic: "Array"
topicSlug: "array"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/combination-sum"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/array/combination_sum.py"
tags: ["Array", "Backtracking"]
patterns: ["Backtracking", "DFS", "Pruning"]
timeComplexity: "O(2^n)"
spaceComplexity: "O(n)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["combination-sum-iv", "subsets", "permutations"]
---

## Problem Statement

Given an array of **distinct** integers `candidates` and a target integer `target`, return _a list of all **unique combinations** of _`candidates`_ where the chosen numbers sum to _`target`_._ You may return the combinations in **any order**.

The **same** number may be chosen from `candidates` an **unlimited number of times**. Two combinations are unique if the frequency of at least one of the chosen numbers is different.

The test cases are generated such that the number of unique combinations that sum up to `target` is less than `150` combinations for the given input.

 

**Example 1:**

```
Input: candidates = [2,3,6,7], target = 7
Output: [[2,2,3],[7]]
Explanation:
2 and 3 are candidates, and 2 + 2 + 3 = 7. Note that 2 can be used multiple times.
7 is a candidate, and 7 = 7.
These are the only two combinations.
```

**Example 2:**

```
Input: candidates = [2,3,5], target = 8
Output: [[2,2,2,2],[2,3,3],[3,5]]
```

**Example 3:**

```
Input: candidates = [2], target = 1
Output: []
```

 

**Constraints:**

- `1 <= candidates.length <= 30`
- `2 <= candidates[i] <= 40`
- All elements of `candidates` are **distinct**.
- `1 <= target <= 40`

## Solution

```python
from typing import List


class Solution:
    def combinationSum(self, candidates: List[int], target: int) -> List[List[int]]:
        def backtrack(start, target, path):
            if target == 0:
                res.append(path)
                return
            for i in range(start, len(candidates)):
                if candidates[i] > target:
                    break
                backtrack(i, target - candidates[i], path + [candidates[i]])

        res = []
        candidates.sort()
        backtrack(0, target, [])
        return res
```


## Explanation

### Approach: Backtracking with Start Index and Pruning

We build combinations recursively while tracking:
- `start`: which candidate indices are allowed next
- `target`: remaining sum
- `path`: current combination

Key choices:
- Reuse is allowed, so recursive call keeps same index `i`
- Candidates are sorted, so if `candidates[i] > target`, we `break` (pruning)

### Step-by-Step Example

Input: `candidates=[2,3,6,7]`, `target=7`

- pick `2` -> remaining `5`
  - pick `2` -> remaining `3`
    - pick `2` -> remaining `1` (prune next choices)
    - pick `3` -> remaining `0` -> record `[2,2,3]`
- pick `3` first path...
- pick `7` -> remaining `0` -> record `[7]`

Result: `[[2,2,3],[7]]`

### Code Breakdown

```python
if target == 0:
    res.append(path)
    return

for i in range(start, len(candidates)):
    if candidates[i] > target:
        break
    backtrack(i, target - candidates[i], path + [candidates[i]])
```

- `backtrack(i, ...)` keeps `i` to allow unlimited reuse
- `path + [x]` creates next combination state

### Why This Works

- Explores all valid non-decreasing index combinations exactly once
- Sorting + pruning avoids unnecessary branches
- Backtracking naturally handles variable combination lengths


## Complexity

- **Time**: `O(2^n)`
- **Space**: `O(n)`


## Similar Problems

- [Combination Sum Iv](/TechInterviewHandbookDSA169/problems/combination-sum-iv)
- [Subsets](/TechInterviewHandbookDSA169/problems/subsets)
- [Permutations](/TechInterviewHandbookDSA169/problems/permutations)
