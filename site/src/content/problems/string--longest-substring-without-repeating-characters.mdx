---
title: "Longest Substring Without Repeating Characters"
problemSlug: "longest-substring-without-repeating-characters"
topic: "String"
topicSlug: "string"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/longest-substring-without-repeating-characters"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/string/lognest_substring_without_repeating_characters.py"
tags: ["Hash Table", "String", "Sliding Window"]
patterns: ["Sliding Window", "Hash Map"]
timeComplexity: "O(n)"
spaceComplexity: "O(min(m,n))"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["minimum-window-substring", "longest-repeating-character-replacement"]
---

## Problem Statement

Given a string `s`, find the length of the **longest** **substring** without duplicate characters.

 

**Example 1:**

```
Input: s = "abcabcbb"
Output: 3
Explanation: The answer is "abc", with the length of 3. Note that "bca" and "cab" are also correct answers.
```

**Example 2:**

```
Input: s = "bbbbb"
Output: 1
Explanation: The answer is "b", with the length of 1.
```

**Example 3:**

```
Input: s = "pwwkew"
Output: 3
Explanation: The answer is "wke", with the length of 3.
Notice that the answer must be a substring, "pwke" is a subsequence and not a substring.
```

 

**Constraints:**

- `0 <= s.length <= 5 * 10^4`
- `s` consists of English letters, digits, symbols and spaces.

## Solution

```python
class Solution:
    def lengthOfLongestSubstring(self, s: str) -> int:
        if not s:
            return 0
        start = 0
        max_len = 0
        used_char = {}
        for i, char in enumerate(s):
            if char in used_char and start <= used_char[char]:
                start = used_char[char] + 1
            else:
                max_len = max(max_len, i - start + 1)
            used_char[char] = i
        return max_len
```


## Explanation

### Approach: Sliding Window + Last Seen Index Map

Maintain a window `[start..i]` with all unique characters.
If current character was seen inside the current window, move `start` right after its previous index.

`used_char` stores the latest index of each character.

### Step-by-Step Example

Input: `"abcabcbb"`

- `i=0, 'a'`: window `"a"`, max=1
- `i=1, 'b'`: window `"ab"`, max=2
- `i=2, 'c'`: window `"abc"`, max=3
- `i=3, 'a'`: duplicate in window, move `start` from 0 to 1
  - window becomes `"bca"`, max stays 3
- `i=4, 'b'`: move `start` to 2
- `i=5, 'c'`: move `start` to 3
- `i=6, 'b'`: move `start` to 5
- `i=7, 'b'`: move `start` to 7

Final answer: 3

### Why `start <= used_char[char]` Matters

It ensures we only move `start` if duplicate is inside current window.
If duplicate is before `start`, we ignore it.

### Code Breakdown

```python
if char in used_char and start <= used_char[char]:
    start = used_char[char] + 1
else:
    max_len = max(max_len, i - start + 1)
used_char[char] = i
```

### Why This is O(n)

Each index is processed once, and `start` only moves forward. Hash map operations are O(1) average.


## Complexity

- **Time**: `O(n)`
- **Space**: `O(min(m,n))`


## Similar Problems

- [Minimum Window Substring](/TechInterviewHandbookDSA169/problems/minimum-window-substring)
- [Longest Repeating Character Replacement](/TechInterviewHandbookDSA169/problems/longest-repeating-character-replacement)
