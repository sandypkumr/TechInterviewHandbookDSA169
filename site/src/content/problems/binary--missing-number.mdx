---
title: "Missing Number"
problemSlug: "missing-number"
topic: "Binary"
topicSlug: "binary"
difficulty: "Easy"
leetcodeUrl: "https://leetcode.com/problems/missing-number"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/binary/missing_number.py"
tags: ["Array", "Hash Table", "Math", "Binary Search", "Bit Manipulation", "Sorting"]
patterns: ["XOR Trick", "Gauss Sum"]
timeComplexity: "O(n)"
spaceComplexity: "O(1)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["find-the-duplicate-number", "single-number"]
---

## Problem Statement

Given an array `nums` containing `n` distinct numbers in the range `[0, n]`, return _the only number in the range that is missing from the array._

 

**Example 1:**

**Input:** nums = [3,0,1]

**Output:** 2

**Explanation:**

`n = 3` since there are 3 numbers, so all numbers are in the range `[0,3]`. 2 is the missing number in the range since it does not appear in `nums`.

**Example 2:**

**Input:** nums = [0,1]

**Output:** 2

**Explanation:**

`n = 2` since there are 2 numbers, so all numbers are in the range `[0,2]`. 2 is the missing number in the range since it does not appear in `nums`.

**Example 3:**

**Input:** nums = [9,6,4,2,3,5,7,0,1]

**Output:** 8

**Explanation:**

`n = 9` since there are 9 numbers, so all numbers are in the range `[0,9]`. 8 is the missing number in the range since it does not appear in `nums`.

 

 

 

 

 

**Constraints:**

- `n == nums.length`
- `1 <= n <= 10^4`
- `0 <= nums[i] <= n`
- All the numbers of `nums` are **unique**.

 

**Follow up:** Could you implement a solution using only `O(1)` extra space complexity and `O(n)` runtime complexity?

## Solution

```python
from typing import List


class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        n = len(nums)
        return n * (n + 1) // 2 - sum(nums)
```


## Explanation

### Approach: Gauss Sum Formula

The numbers should contain every value from `0` to `n`, except one missing value.
So:

`missing = (expected sum from 0..n) - (actual sum of array)`

### Formula Used

Sum of first `n` integers:

`0 + 1 + 2 + ... + n = n * (n + 1) // 2`

### Step-by-Step Example

**Input:** `nums = [3,0,1]`

- `n = 3`
- expected sum = `3 * 4 // 2 = 6`
- actual sum = `3 + 0 + 1 = 4`
- missing = `6 - 4 = 2`

Answer: `2`

### Code Breakdown

```python
n = len(nums)
return n * (n + 1) // 2 - sum(nums)
```

- `n*(n+1)//2`: ideal total if nothing is missing
- `sum(nums)`: observed total
- difference is exactly the missing number

### Why This Works

- Array has distinct values in `[0, n]`
- Exactly one value is absent
- So expected total differs from actual total by exactly that number

This gives O(n) time (for `sum(nums)`) and O(1) extra space.


## Complexity

- **Time**: `O(n)`
- **Space**: `O(1)`


## Similar Problems

- [Find The Duplicate Number](/TechInterviewHandbookDSA169/problems/find-the-duplicate-number)
- [Single Number](/TechInterviewHandbookDSA169/problems/single-number)
