---
title: "Letter Combinations of a Phone Number"
problemSlug: "letter-combinations-of-a-phone-number"
topic: "Recursion"
topicSlug: "recursion"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/letter-combinations-of-a-phone-number"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/recursion/letter_combinations_of_a_phone_number.py"
tags: ["Hash Table", "String", "Backtracking"]
patterns: ["Backtracking", "DFS"]
timeComplexity: "O(4^n)"
spaceComplexity: "O(n)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["generate-parentheses"]
---

## Problem Statement

Given a string containing digits from `2-9` inclusive, return all possible letter combinations that the number could represent. Return the answer in **any order**.

A mapping of digits to letters (just like on the telephone buttons) is given below. Note that 1 does not map to any letters.

 

**Example 1:**

```
Input: digits = "23"
Output: ["ad","ae","af","bd","be","bf","cd","ce","cf"]
```

**Example 2:**

```
Input: digits = "2"
Output: ["a","b","c"]
```

 

**Constraints:**

- `1 <= digits.length <= 4`
- `digits[i]` is a digit in the range `['2', '9']`.

## Solution

## Explanation

### Approach: Backtracking by Digit Index

Each digit maps to several letters (phone keypad). We choose one letter per digit in order.

Recursive state:
- `start`: current digit index
- `path`: built string so far

When `start == len(digits)`, one full combination is complete.

### Example (`"23"`)

- digit `2` -> `a,b,c`
- for each, append one of digit `3` -> `d,e,f`

Outputs: `ad, ae, af, bd, be, bf, cd, ce, cf`.

### Code Core

```python
for c in phone[digits[start]]:
    backtrack(start + 1, path + c)
```

### Why This Works

- Recursion explores Cartesian product of letter sets across digit positions
- Every valid combination appears once
- Empty input handled directly by returning `[]`
When `start == len(digits)`, one full combination is complete.

### Example (`digits = "23"`)

- digit `2` -> `a,b,c`
- for each, combine with digit `3` -> `d,e,f`

Produces:
`ad, ae, af, bd, be, bf, cd, ce, cf`

### Code Core

```python
for c in phone[digits[start]]:
    backtrack(start + 1, path + c)
```

`phone` dictionary provides letter options for each digit.

### Why This Works

- Recursion explores Cartesian product of letter sets by position
- Every valid combination is generated exactly once
- Empty input handled explicitly with `return []`


## Complexity

- **Time**: `O(4^n)`
- **Space**: `O(n)`


## Similar Problems

- [Generate Parentheses](/TechInterviewHandbookDSA169/problems/generate-parentheses)
