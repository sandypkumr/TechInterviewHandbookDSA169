---
title: "Climbing Stairs"
problemSlug: "climbing-stairs"
topic: "Dynamic Programming"
topicSlug: "dynamic-programming"
difficulty: "Easy"
leetcodeUrl: "https://leetcode.com/problems/climbing-stairs"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/dynamic_programming/climbing_stairs.py"
tags: ["Math", "Dynamic Programming", "Memoization"]
patterns: ["Dynamic Programming", "Fibonacci Variant"]
timeComplexity: "O(n)"
spaceComplexity: "O(1)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["unique-paths", "house-robber"]
---

## Problem Statement

You are climbing a staircase. It takes `n` steps to reach the top.

Each time you can either climb `1` or `2` steps. In how many distinct ways can you climb to the top?

 

**Example 1:**

```
Input: n = 2
Output: 2
Explanation: There are two ways to climb to the top.
1. 1 step + 1 step
2. 2 steps
```

**Example 2:**

```
Input: n = 3
Output: 3
Explanation: There are three ways to climb to the top.
1. 1 step + 1 step + 1 step
2. 1 step + 2 steps
3. 2 steps + 1 step
```

 

**Constraints:**

- `1 <= n <= 45`

## Solution

```python
class Solution:
    def climbStairs(self, n: int) -> int:
        if n == 1:
            return 1
        first = 1
        second = 2
        for i in range(3, n + 1):
            third = first + second
            first = second
            second = third
        return second
```


## Explanation

### Approach: Fibonacci-Style Dynamic Programming

Let `ways[i]` be the number of ways to reach step `i`.

To reach step `i`, your last move must be either:
- from `i-1` (take 1 step), or
- from `i-2` (take 2 steps)

So recurrence is:

`ways[i] = ways[i-1] + ways[i-2]`

This is exactly Fibonacci-style DP.

### Step-by-Step Example

For `n = 5`:

- `ways[1] = 1`
- `ways[2] = 2`
- `ways[3] = 3`
- `ways[4] = 5`
- `ways[5] = 8`

Answer is `8`.

### Why This Code Uses O(1) Space

We only need the previous two states at any point, so no full DP array is required.

### Code Breakdown

```python
if n == 1:
    return 1
first = 1    # ways to reach step 1
second = 2   # ways to reach step 2
for i in range(3, n + 1):
    third = first + second
    first = second
    second = third
return second
```

- `first` = `ways[i-2]`
- `second` = `ways[i-1]`
- `third` = `ways[i]`

### Why This Works

- Correct recurrence from problem structure (last jump 1 or 2)
- Bottom-up iteration guarantees all dependencies are ready
- O(n) time and O(1) extra space


## Complexity

- **Time**: `O(n)`
- **Space**: `O(1)`


## Similar Problems

- [Unique Paths](/TechInterviewHandbookDSA169/problems/unique-paths)
- [House Robber](/TechInterviewHandbookDSA169/problems/house-robber)
