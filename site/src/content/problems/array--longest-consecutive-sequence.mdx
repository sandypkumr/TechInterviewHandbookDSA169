---
title: "Longest Consecutive Sequence"
problemSlug: "longest-consecutive-sequence"
topic: "Array"
topicSlug: "array"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/longest-consecutive-sequence"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/array/longest_consecutive_sequence.py"
tags: ["Array", "Hash Table", "Union Find"]
patterns: ["Hash Set", "Sequence Building"]
timeComplexity: "O(n)"
spaceComplexity: "O(n)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["contains-duplicate"]
---

## Problem Statement

Given an unsorted array of integers `nums`, return _the length of the longest consecutive elements sequence._

You must write an algorithm that runs in `O(n)` time.

 

**Example 1:**

```
Input: nums = [100,4,200,1,3,2]
Output: 4
Explanation: The longest consecutive elements sequence is [1, 2, 3, 4]. Therefore its length is 4.
```

**Example 2:**

```
Input: nums = [0,3,7,2,5,8,4,6,0,1]
Output: 9
```

**Example 3:**

```
Input: nums = [1,0,1,2]
Output: 3
```

 

**Constraints:**

- `0 <= nums.length <= 10^5`
- `-10^9 <= nums[i] <= 10^9`

## Solution

```python
from typing import List


class Solution:
    def longestConsecutive(self, nums: List[int]) -> int:
        num_set = set(nums)
        longest_streak = 0
        for num in num_set:
            if num - 1 not in num_set:
                current_num = num
                current_streak = 1
                while current_num + 1 in num_set:
                    current_num += 1
                    current_streak += 1
                longest_streak = max(longest_streak, current_streak)
        return longest_streak
```


## Explanation

### Approach: Hash Set + Start-of-Sequence Detection

Put all numbers in a set for O(1) membership checks. Then only start counting from numbers that are the **beginning** of a sequence (i.e., `num - 1` is not present).

This avoids re-counting the same sequence from every element.

### Key Insight

If `num - 1` exists, then `num` is not the start. So skip it.
Only expand from true starts.

### Step-by-Step Walkthrough

**Input:** `[100,4,200,1,3,2]`

Set = `{100,4,200,1,3,2}`

- `100`: `99` not in set -> start sequence -> `100` length = 1
- `4`: `3` in set -> not a start, skip
- `200`: `199` not in set -> start sequence -> `200` length = 1
- `1`: `0` not in set -> start sequence -> `1,2,3,4` length = 4

Longest = 4

### Code Breakdown

```python
num_set = set(nums)
for num in num_set:
    if num - 1 not in num_set:   # start of a sequence
        current_num = num
        current_streak = 1
        while current_num + 1 in num_set:
            current_num += 1
            current_streak += 1
```

- Set gives O(1) checks
- Each number is effectively expanded once across all sequences

### Why This is O(n)

Even though there is a `while`, each number participates in at most one forward expansion from a start point. So total work remains linear.


## Complexity

- **Time**: `O(n)`
- **Space**: `O(n)`


## Similar Problems

- [Contains Duplicate](/TechInterviewHandbookDSA169/problems/contains-duplicate)
