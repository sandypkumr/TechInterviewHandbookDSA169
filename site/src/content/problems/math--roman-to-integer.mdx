---
title: "Roman to Integer"
problemSlug: "roman-to-integer"
topic: "Math"
topicSlug: "math"
difficulty: "Easy"
leetcodeUrl: "https://leetcode.com/problems/roman-to-integer"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/math/roman_to_integer.py"
tags: ["Hash Table", "Math", "String"]
patterns: ["Hash Map", "Symbol Comparison"]
timeComplexity: "O(1)"
spaceComplexity: "O(1)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: []
---

## Problem Statement

Roman numerals are represented by seven different symbols: `I`, `V`, `X`, `L`, `C`, `D` and `M`.

```
Symbol Value
I 1
V 5
X 10
L 50
C 100
D 500
M 1000
```

For example, `2` is written as `II` in Roman numeral, just two ones added together. `12` is written as `XII`, which is simply `X + II`. The number `27` is written as `XXVII`, which is `XX + V + II`.

Roman numerals are usually written largest to smallest from left to right. However, the numeral for four is not `IIII`. Instead, the number four is written as `IV`. Because the one is before the five we subtract it making four. The same principle applies to the number nine, which is written as `IX`. There are six instances where subtraction is used:

- `I` can be placed before `V` (5) and `X` (10) to make 4 and 9. 
- `X` can be placed before `L` (50) and `C` (100) to make 40 and 90. 
- `C` can be placed before `D` (500) and `M` (1000) to make 400 and 900.

Given a roman numeral, convert it to an integer.

 

**Example 1:**

```
Input: s = "III"
Output: 3
Explanation: III = 3.
```

**Example 2:**

```
Input: s = "LVIII"
Output: 58
Explanation: L = 50, V= 5, III = 3.
```

**Example 3:**

```
Input: s = "MCMXCIV"
Output: 1994
Explanation: M = 1000, CM = 900, XC = 90 and IV = 4.
```

 

**Constraints:**

- `1 <= s.length <= 15`
- `s` contains only the characters `('I', 'V', 'X', 'L', 'C', 'D', 'M')`.
- It is **guaranteed** that `s` is a valid roman numeral in the range `[1, 3999]`.

## Solution

```python
class Solution:
    def romanToInt(self, s: str) -> int:
        roman_to_int = {
            'I': 1,
            'V': 5,
            'X': 10,
            'L': 50,
            'C': 100,
            'D': 500,
            'M': 1000
        }
        result = 0
        prev = 0
        for c in s:
            curr = roman_to_int[c]
            result += curr
            if curr > prev:
                result -= 2 * prev
            prev = curr
        return result
```


## Explanation

### Approach: Hash Map Lookup

This solution uses a **hash map** for symbol values and scans the Roman numeral once from left to right.

### Intuition

Most Roman numerals are additive, but a smaller value before a larger one means subtraction, as in `IV` or `IX`. By remembering the previous value while scanning, we can detect those subtractive pairs and adjust the running total correctly.

### Algorithm

Here's the step-by-step approach:

1. Create a map from Roman symbols to their integer values.
2. Traverse the string from left to right, converting each character to its value.
3. Add the current value to the result, but if it is larger than the previous value, subtract twice the previous value to undo the earlier addition and apply the subtractive rule.
4. Return the final accumulated total.

### Code Breakdown

`roman_to_int` provides constant-time access to each symbol's value. The variable `prev` keeps track of the previous numeral so the code can recognize patterns like `IV`, `XL`, or `CM` without extra lookahead logic.

### Why This Works

- **Additive baseline:** Every numeral is added once by default.
- **Subtractive correction:** When a larger numeral follows a smaller one, subtracting `2 * prev` converts the earlier addition into the correct subtraction.
- **Single-pass evaluation:** The algorithm processes each symbol once while maintaining exactly the information needed for Roman numeral rules.


## Complexity

- **Time**: `O(1)`
- **Space**: `O(1)`
