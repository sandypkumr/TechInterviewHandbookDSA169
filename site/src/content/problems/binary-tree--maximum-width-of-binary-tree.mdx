---
title: "Maximum Width of Binary Tree"
problemSlug: "maximum-width-of-binary-tree"
topic: "Binary Tree"
topicSlug: "binary-tree"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/maximum-width-of-binary-tree"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/binary_tree/maximum_width_of_binary_tree.py"
tags: ["Tree", "Depth-First Search", "Breadth-First Search", "Binary Tree"]
patterns: ["BFS Level Order", "Node Indexing"]
timeComplexity: "O(n)"
spaceComplexity: "O(n)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["binary-tree-level-order-traversal"]
---

## Problem Statement

Given the `root` of a binary tree, return _the **maximum width** of the given tree_.

The **maximum width** of a tree is the maximum **width** among all levels.

The **width** of one level is defined as the length between the end-nodes (the leftmost and rightmost non-null nodes), where the null nodes between the end-nodes that would be present in a complete binary tree extending down to that level are also counted into the length calculation.

It is **guaranteed** that the answer will in the range of a **32-bit** signed integer.

 

**Example 1:**

```
Input: root = [1,3,2,5,3,null,9]
Output: 4
Explanation: The maximum width exists in the third level with length 4 (5,3,null,9).
```

**Example 2:**

```
Input: root = [1,3,2,5,null,null,9,6,null,7]
Output: 7
Explanation: The maximum width exists in the fourth level with length 7 (6,null,null,null,null,null,7).
```

**Example 3:**

```
Input: root = [1,3,2,5]
Output: 2
Explanation: The maximum width exists in the second level with length 2 (3,2).
```

 

**Constraints:**

- The number of nodes in the tree is in the range `[1, 3000]`.
- `-100 <= Node.val <= 100`

## Solution

```python
from collections import deque
from typing import Optional


class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right


class Solution:
    def widthOfBinaryTree(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0
        res = 0
        q = deque([(root, 0)])
        while q:
            res = max(res, q[-1][1] - q[0][1] + 1)
            new_q = deque()
            for node, idx in q:
                if node.left:
                    new_q.append((node.left, idx * 2))
                if node.right:
                    new_q.append((node.right, idx * 2 + 1))
            q = new_q
        return res
```


## Explanation

### Approach: BFS

This solution uses **Breadth-First Search (BFS)** with a queue to explore states level by level.

### Intuition

The width of a level depends on where its leftmost and rightmost nodes would sit in a complete binary tree, not just on how many nodes exist. By carrying an index with each node during BFS, we can compute the width of every level as `last_index - first_index + 1`.

### Algorithm

Here's the step-by-step approach:

1. Return `0` if the tree is empty.
2. Start BFS with the root assigned index `0`.
3. For each level, compute its width from the first and last indices currently in the queue.
4. Push children into the next level with indices `2 * idx` and `2 * idx + 1`, and keep the maximum width seen.

### Code Breakdown

Each queue entry stores `(node, idx)`, where `idx` represents the node's position in an imaginary complete binary tree. That lets the algorithm measure gaps between nodes correctly, even when some children are missing

### Why This Works

- **Positional indexing:** The complete-tree indices preserve the horizontal distance between nodes on the same level.
- **Correct width formula:** `last - first + 1` counts all slots between the outermost nodes, which matches the problem definition.
- **Level-order traversal:** BFS ensures the first and last queued indices belong to the same depth when width is computed.

### Why This Works

- **Level-by-level exploration:** Guarantees we find the shortest path (if it's a path problem) and systematically covers all nodes
- **Logarithmic search:** By eliminating half the search space with each iteration, we achieve O(log n) time complexity
- **No nested loops:** Two pointers avoid O(n²) complexity by moving intelligently through the data
- **Handles edge cases:** Correctly processes boundary conditions and special inputs
- **Time-space optimization:** Balances time complexity gains with reasonable space usage


## Complexity

- **Time**: `O(n)`
- **Space**: `O(n)`


## Similar Problems

- [Binary Tree Level Order Traversal](/TechInterviewHandbookDSA169/problems/binary-tree-level-order-traversal)
