---
title: "Inorder Successor in BST"
problemSlug: "inorder-successor-in-bst"
topic: "Binary Search Tree"
topicSlug: "binary-search-tree"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/inorder-successor-in-bst"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/binary_search_tree/inorder_successor_in_bst.py"
tags: ["Tree", "Binary Search Tree", "Binary Tree"]
patterns: ["BST Property", "Iterative In-order"]
timeComplexity: "O(h)"
spaceComplexity: "O(1)"
hasProblemStatement: true
hasConstraints: false
hasExamples: false
isPremium: true
similarProblems: ["kth-smallest-element-in-a-bst"]
---

## Problem Statement

Given the root of a binary search tree and a node p in it, return the in-order successor of that node in the BST.
    If the given node has no in-order successor in the tree, return null.

    The successor of a node p is the node with the smallest key greater than p.val.

    Example 1:

    Input: root = [2,1,3], p = 1
    Output: 2

    Example 2:

    Input: root = [5,3,6,2,4,null,null,1], p = 6
    Output: null

    Constraints:

    The number of nodes in the tree is in the range [1, 104].
    -105 \<= Node.val \<= 105
    All Nodes will have unique values.


## Solution

```python
from typing import Optional


class TreeNode:
    def __init__(self, x):
        self.val = x
        self.left = None
        self.right = None


class Solution:
    def inorderSuccessor(self, root: Optional['TreeNode'], p: Optional['TreeNode']) -> Optional['TreeNode']:
        ans = None
        while root:
            if root.val > p.val:
                ans = root
                root = root.left
            else:
                root = root.right
        return ans
```


## Explanation

### Approach: BST Search with Successor Candidate

We want the smallest value greater than `p.val`.

Traverse from root:
- if `root.val > p.val`, root is a valid successor candidate; go left to find smaller valid one
- else (`root.val <= p.val`), successor must be on right

Keep best candidate in `ans`.

### Code Logic

```python
ans = None
while root:
    if root.val > p.val:
        ans = root
        root = root.left
    else:
        root = root.right
return ans
```

### Why This Works

- BST ordering guarantees pruning of irrelevant subtrees
- `ans` always stores the best (smallest-so-far) value greater than `p.val`
- If no greater value exists, `ans` stays `None`

Time is O(h), where `h` is tree height.


## Complexity

- **Time**: `O(h)`
- **Space**: `O(1)`


## Similar Problems

- [Kth Smallest Element In A Bst](/TechInterviewHandbookDSA169/problems/kth-smallest-element-in-a-bst)
