---
title: "Meeting Rooms II"
problemSlug: "meeting-rooms-ii"
topic: "Array"
topicSlug: "array"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/meeting-rooms-ii"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/array/meeting_rooms_2.py"
tags: ["Array", "Two Pointers", "Greedy", "Sorting", "Heap"]
patterns: ["Min Heap", "Two Arrays Sort", "Sweep Line"]
timeComplexity: "O(n log n)"
spaceComplexity: "O(n)"
hasProblemStatement: true
hasConstraints: false
hasExamples: true
isPremium: true
similarProblems: ["meeting-rooms", "merge-intervals"]
---

## Problem Statement

Given an array of meeting time intervals, where each interval is represented as [start, end], find the minimum number
of meeting rooms required to host all the meetings.


## Examples

For example:

Input: [[0, 30], [5, 10], [15, 20]]
Output: 2


## Solution

```python
from typing import List


class Interval(object):
    def __init__(self, start, end):
        self.start = start
        self.end = end

class Solution:
    def minMeetingRooms(self, intervals: List[Interval]) -> int:
        if not intervals:
            return 0
        start_times = sorted([interval.start for interval in intervals])
        end_times = sorted([interval.end for interval in intervals])
        start_pointer = end_pointer = 0
        num_rooms = res = 0
        while start_pointer < len(start_times):
            if start_times[start_pointer] < end_times[end_pointer]:
                num_rooms += 1
                start_pointer += 1
            else:
                num_rooms -= 1
                end_pointer += 1
            res = max(res, num_rooms)
        return res
```


## Explanation

### Approach: Sweep Line with Sorted Start/End Arrays

Split intervals into:
- `start_times` sorted
- `end_times` sorted

Then sweep through time:
- if next meeting starts before earliest ending meeting ends, need a new room
- otherwise one meeting ended, so a room is freed

Track maximum simultaneous rooms.

### Step-by-Step Example

Input: `[[0,30],[5,10],[15,20]]`

- `starts = [0,5,15]`
- `ends   = [10,20,30]`

Sweep:
- start `0 < 10` -> rooms=1
- start `5 < 10` -> rooms=2
- start `15 >= 10` -> free one (rooms=1), then occupy for 15 -> rooms=2

Maximum rooms needed = 2.

### Code Breakdown

```python
start_pointer = end_pointer = 0
num_rooms = res = 0
while start_pointer < len(start_times):
    if start_times[start_pointer] < end_times[end_pointer]:
        num_rooms += 1
        start_pointer += 1
    else:
        num_rooms -= 1
        end_pointer += 1
    res = max(res, num_rooms)
```

### Why This Works

- Comparing next start with earliest end simulates room allocation greedily
- Sorted arrays ensure chronological processing
- Maximum concurrent meetings equals minimum rooms required


## Complexity

- **Time**: `O(n log n)`
- **Space**: `O(n)`


## Similar Problems

- [Meeting Rooms](/TechInterviewHandbookDSA169/problems/meeting-rooms)
- [Merge Intervals](/TechInterviewHandbookDSA169/problems/merge-intervals)
