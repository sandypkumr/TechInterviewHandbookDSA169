---
title: "Course Schedule II"
problemSlug: "course-schedule-ii"
topic: "Graph"
topicSlug: "graph"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/course-schedule-ii"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/graph/course_schedule_2.py"
tags: ["Depth-First Search", "Breadth-First Search", "Graph", "Topological Sort"]
patterns: ["Topological Sort", "Kahn's Algorithm", "BFS"]
timeComplexity: "O(V+E)"
spaceComplexity: "O(V+E)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["course-schedule", "alien-dictionary"]
---

## Problem Statement

There are a total of `numCourses` courses you have to take, labeled from `0` to `numCourses - 1`. You are given an array `prerequisites` where `prerequisites[i] = [a_i, b_i]` indicates that you **must** take course `b_i` first if you want to take course `a_i`.

- For example, the pair `[0, 1]`, indicates that to take course `0` you have to first take course `1`.

Return _the ordering of courses you should take to finish all courses_. If there are many valid answers, return **any** of them. If it is impossible to finish all courses, return **an empty array**.

 

**Example 1:**

```
Input: numCourses = 2, prerequisites = [[1,0]]
Output: [0,1]
Explanation: There are a total of 2 courses to take. To take course 1 you should have finished course 0. So the correct course order is [0,1].
```

**Example 2:**

```
Input: numCourses = 4, prerequisites = [[1,0],[2,0],[3,1],[3,2]]
Output: [0,2,1,3]
Explanation: There are a total of 4 courses to take. To take course 3 you should have finished both courses 1 and 2. Both courses 1 and 2 should be taken after you finished course 0.
So one correct course order is [0,1,2,3]. Another correct ordering is [0,2,1,3].
```

**Example 3:**

```
Input: numCourses = 1, prerequisites = []
Output: [0]
```

 

**Constraints:**

- `1 <= numCourses <= 2000`
- `0 <= prerequisites.length <= numCourses * (numCourses - 1)`
- `prerequisites[i].length == 2`
- `0 <= a_i, b_i < numCourses`
- `a_i != b_i`
- All the pairs `[a_i, b_i]` are **distinct**.

## Solution

```python
from typing import List


class Solution:
    def findOrder(self, numCourses: int, prerequisites: List[List[int]]) -> List[int]:
        graph = {i: [] for i in range(numCourses)}
        indegree = {i: 0 for i in range(numCourses)}
        for u, v in prerequisites:
            graph[v].append(u)
            indegree[u] += 1
        queue = [i for i in indegree if indegree[i] == 0]
        order = []
        while queue:
            node = queue.pop(0)
            order.append(node)
            for neighbor in graph[node]:
                indegree[neighbor] -= 1
                if indegree[neighbor] == 0:
                    queue.append(neighbor)
        return order if len(order) == numCourses else []
```


## Explanation

### Approach: Topological Sort (Kahn's Algorithm)

To produce a valid course order, we perform topological sorting on the prerequisite graph.

- Edge `v -> u` means: take `v` before `u`
- `indegree[u]` = number of prerequisites still needed for course `u`

Start with all courses of indegree 0 (no prerequisites). Repeatedly take one, add to answer, and reduce indegree of its neighbors.

### Step-by-Step Example

`numCourses=4`, prerequisites `[[1,0],[2,0],[3,1],[3,2]]`

- indegree: `0:0, 1:1, 2:1, 3:2`
- queue starts `[0]`
- pop `0` -> order `[0]`, indegree of `1,2` becomes 0 -> queue `[1,2]`
- pop `1` -> order `[0,1]`, indegree of `3` becomes 1
- pop `2` -> order `[0,1,2]`, indegree of `3` becomes 0 -> queue `[3]`
- pop `3` -> order `[0,1,2,3]`

All courses scheduled -> valid answer.

### Code Breakdown

```python
for u, v in prerequisites:
    graph[v].append(u)
    indegree[u] += 1

queue = [i for i in indegree if indegree[i] == 0]
while queue:
    node = queue.pop(0)
    order.append(node)
    for neighbor in graph[node]:
        indegree[neighbor] -= 1
        if indegree[neighbor] == 0:
            queue.append(neighbor)
```

If `len(order) < numCourses`, a cycle exists -> return `[]`.

### Why This Works

- Kahn's algorithm returns a topological order iff graph is acyclic
- Exactly captures prerequisite constraints
- Detects impossible cases via unfinished nodes


## Complexity

- **Time**: `O(V+E)`
- **Space**: `O(V+E)`


## Similar Problems

- [Course Schedule](/TechInterviewHandbookDSA169/problems/course-schedule)
- [Alien Dictionary](/TechInterviewHandbookDSA169/problems/alien-dictionary)
