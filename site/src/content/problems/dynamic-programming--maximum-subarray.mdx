---
title: "Maximum Subarray"
problemSlug: "maximum-subarray"
topic: "Dynamic Programming"
topicSlug: "dynamic-programming"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/maximum-subarray"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/dynamic_programming/maximum_subarray.py"
tags: ["Array", "Divide and Conquer", "Dynamic Programming"]
patterns: ["Kadane's Algorithm", "Dynamic Programming"]
timeComplexity: "O(n)"
spaceComplexity: "O(1)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["maximum-product-subarray", "house-robber"]
---

## Problem Statement

Given an integer array `nums`, find the subarray with the largest sum, and return _its sum_.

 

**Example 1:**

```
Input: nums = [-2,1,-3,4,-1,2,1,-5,4]
Output: 6
Explanation: The subarray [4,-1,2,1] has the largest sum 6.
```

**Example 2:**

```
Input: nums = [1]
Output: 1
Explanation: The subarray [1] has the largest sum 1.
```

**Example 3:**

```
Input: nums = [5,4,-1,7,8]
Output: 23
Explanation: The subarray [5,4,-1,7,8] has the largest sum 23.
```

 

**Constraints:**

- `1 <= nums.length <= 10^5`
- `-10^4 <= nums[i] <= 10^4`

 

**Follow up:** If you have figured out the `O(n)` solution, try coding another solution using the **divide and conquer** approach, which is more subtle.

## Solution

```python
from typing import List


class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        max_sum = nums[0]
        current_sum = 0
        for i in range(len(nums)):
            if current_sum < 0:
                current_sum = 0
            current_sum = current_sum + nums[i]
            max_sum = max(max_sum, current_sum)
        return max_sum
```


## Complexity

- **Time**: `O(n)`
- **Space**: `O(1)`


## Similar Problems

- [Maximum Product Subarray](/TechInterviewHandbookDSA169/problems/maximum-product-subarray)
- [House Robber](/TechInterviewHandbookDSA169/problems/house-robber)
