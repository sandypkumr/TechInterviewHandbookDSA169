---
title: "Valid Palindrome"
problemSlug: "valid-palindrome"
topic: "String"
topicSlug: "string"
difficulty: "Easy"
leetcodeUrl: "https://leetcode.com/problems/valid-palindrome"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/string/valid_palindrome.py"
tags: ["Two Pointers", "String"]
patterns: ["Two Pointers"]
timeComplexity: "O(n)"
spaceComplexity: "O(n)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["palindrome-linked-list", "longest-palindromic-substring"]
---

## Problem Statement

A phrase is a **palindrome** if, after converting all uppercase letters into lowercase letters and removing all non-alphanumeric characters, it reads the same forward and backward. Alphanumeric characters include letters and numbers.

Given a string `s`, return `true`_ if it is a **palindrome**, or _`false`_ otherwise_.

 

**Example 1:**

```
Input: s = "A man, a plan, a canal: Panama"
Output: true
Explanation: "amanaplanacanalpanama" is a palindrome.
```

**Example 2:**

```
Input: s = "race a car"
Output: false
Explanation: "raceacar" is not a palindrome.
```

**Example 3:**

```
Input: s = " "
Output: true
Explanation: s is an empty string "" after removing non-alphanumeric characters.
Since an empty string reads the same forward and backward, it is a palindrome.
```

 

**Constraints:**

- `1 <= s.length <= 2 * 10^5`
- `s` consists only of printable ASCII characters.

## Solution

```python
class Solution:
    def isPalindrome(self, s: str) -> bool:
        s = ''.join(filter(str.isalnum, s)).lower()
        return s == s[::-1]
```


## Explanation

### Approach: Normalize Then Compare with Reverse

This implementation first builds a cleaned lowercase string containing only alphanumeric characters, then checks whether it equals its reverse.

### Step-by-Step Example

Input: `"A man, a plan, a canal: Panama"`

1. Filter alphanumeric only -> `"AmanaplanacanalPanama"`
2. Lowercase -> `"amanaplanacanalpanama"`
3. Compare with reverse:
   - original: `amanaplanacanalpanama`
   - reverse:  `amanaplanacanalpanama`

Equal -> palindrome (`true`).

### Code Breakdown

```python
s = ''.join(filter(str.isalnum, s)).lower()
return s == s[::-1]
```

- `filter(str.isalnum, s)`: keeps only letters/digits
- `.lower()`: case-insensitive comparison
- `s[::-1]`: reversed string

### Why This Works

- Problem definition explicitly ignores case and non-alphanumeric chars
- Normalization transforms input into canonical form
- Palindrome check then becomes direct string equality vs reversed string


## Complexity

- **Time**: `O(n)`
- **Space**: `O(n)`


## Similar Problems

- [Palindrome Linked List](/TechInterviewHandbookDSA169/problems/palindrome-linked-list)
- [Longest Palindromic Substring](/TechInterviewHandbookDSA169/problems/longest-palindromic-substring)
