---
title: "Decode Ways"
problemSlug: "decode-ways"
topic: "Dynamic Programming"
topicSlug: "dynamic-programming"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/decode-ways"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/dynamic_programming/decode_ways.py"
tags: ["String", "Dynamic Programming"]
patterns: ["Dynamic Programming", "1D DP", "String Parsing"]
timeComplexity: "O(n)"
spaceComplexity: "O(n)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["climbing-stairs"]
---

## Problem Statement

You have intercepted a secret message encoded as a string of numbers. The message is **decoded** via the following mapping:

`"1" -> 'A'
"2" -> 'B'
...
"25" -> 'Y'
"26" -> 'Z'`

However, while decoding the message, you realize that there are many different ways you can decode the message because some codes are contained in other codes (`"2"` and `"5"` vs `"25"`).

For example, `"11106"` can be decoded into:

- `"AAJF"` with the grouping `(1, 1, 10, 6)`
- `"KJF"` with the grouping `(11, 10, 6)`
- The grouping `(1, 11, 06)` is invalid because `"06"` is not a valid code (only `"6"` is valid).

Note: there may be strings that are impossible to decode.

Given a string s containing only digits, return the **number of ways** to **decode** it. If the entire string cannot be decoded in any valid way, return `0`.

The test cases are generated so that the answer fits in a **32-bit** integer.

 

**Example 1:**

**Input:** s = "12"

**Output:** 2

**Explanation:**

"12" could be decoded as "AB" (1 2) or "L" (12).

**Example 2:**

**Input:** s = "226"

**Output:** 3

**Explanation:**

"226" could be decoded as "BZ" (2 26), "VF" (22 6), or "BBF" (2 2 6).

**Example 3:**

**Input:** s = "06"

**Output:** 0

**Explanation:**

"06" cannot be mapped to "F" because of the leading zero ("6" is different from "06"). In this case, the string is not a valid encoding, so return 0.

 

**Constraints:**

- `1 <= s.length <= 100`
- `s` contains only digits and may contain leading zero(s).

## Solution

```python
class Solution:
    def numDecodings(self, s: str) -> int:
        if not s:
            return 0
        prev2, prev1 = 1, 1 if s[0] != '0' else 0
        for i in range(1, len(s)):
            curr = 0
            if s[i] != '0':
                curr += prev1
            if '10' <= s[i - 1:i + 1] <= '26':
                curr += prev2
            prev2, prev1 = prev1, curr
        return prev1
```


## Explanation

### Approach: 1D DP with Rolling Variables

At each position `i`, number of decodings depends on:
1. Using `s[i]` as a single digit (valid if `s[i] != '0'`)
2. Using `s[i-1:i+1]` as two digits (valid if between `10` and `26`)

So:

`ways[i] = (single-digit valid ? ways[i-1] : 0) + (two-digit valid ? ways[i-2] : 0)`

The code stores only two previous states: `prev1 = ways[i-1]`, `prev2 = ways[i-2]`.

### Step-by-Step Example

Input: `s = "226"`

- Start: `prev2=1`, `prev1=1` (`"2"` is valid)
- `i=1` (`"2"` and `"22"`): both valid
  - `curr = prev1 + prev2 = 1 + 1 = 2`
- `i=2` (`"6"` and `"26"`): both valid
  - `curr = prev1 + prev2 = 2 + 1 = 3`

Answer = 3 (`"2 2 6"`, `"22 6"`, `"2 26"`)

### Code Breakdown

```python
if s[i] != '0':
    curr += prev1
if '10' <= s[i - 1:i + 1] <= '26':
    curr += prev2
prev2, prev1 = prev1, curr
```

### Why This Works

- Recurrence covers all valid decoding choices at each position
- Invalid patterns (like leading zero) contribute 0 naturally
- One pass, constant extra state


## Complexity

- **Time**: `O(n)`
- **Space**: `O(1)`


## Similar Problems

- [Climbing Stairs](/TechInterviewHandbookDSA169/problems/climbing-stairs)
