---
title: "Asteroid Collision"
problemSlug: "asteroid-collision"
topic: "Stack"
topicSlug: "stack"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/asteroid-collision"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/stack/asteroid_collision.py"
tags: ["Array", "Stack", "Simulation"]
patterns: ["Monotonic Stack", "Simulation"]
timeComplexity: "O(n)"
spaceComplexity: "O(n)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["daily-temperatures"]
---

## Problem Statement

We are given an array `asteroids` of integers representing asteroids in a row. The indices of the asteroid in the array represent their relative position in space.

For each asteroid, the absolute value represents its size, and the sign represents its direction (positive meaning right, negative meaning left). Each asteroid moves at the same speed.

Find out the state of the asteroids after all collisions. If two asteroids meet, the smaller one will explode. If both are the same size, both will explode. Two asteroids moving in the same direction will never meet.

 

**Example 1:**

```
Input: asteroids = [5,10,-5]
Output: [5,10]
Explanation: The 10 and -5 collide resulting in 10. The 5 and 10 never collide.
```

**Example 2:**

```
Input: asteroids = [8,-8]
Output: []
Explanation: The 8 and -8 collide exploding each other.
```

**Example 3:**

```
Input: asteroids = [10,2,-5]
Output: [10]
Explanation: The 2 and -5 collide resulting in -5. The 10 and -5 collide resulting in 10.
```

**Example 4:**

```
Input: asteroids = [3,5,-6,2,-1,4]​​​​​​​
Output: [-6,2,4]
Explanation: The asteroid -6 makes the asteroid 3 and 5 explode, and then continues going left. On the other side, the asteroid 2 makes the asteroid -1 explode and then continues going right, without reaching asteroid 4.
```

 

**Constraints:**

- `2 <= asteroids.length <= 10^4`
- `-1000 <= asteroids[i] <= 1000`
- `asteroids[i] != 0`

## Solution

```python
from typing import List


class Solution:
    def asteroidCollision(self, asteroids: List[int]) -> List[int]:
        stack = []
        for asteroid in asteroids:
            while stack and asteroid < 0 < stack[-1]:
                if stack[-1] < -asteroid:
                    stack.pop()
                    continue
                elif stack[-1] == -asteroid:
                    stack.pop()
                break
            else:
                stack.append(asteroid)
        return stack
```


## Explanation

### Approach: Monotonic Stack

This solution uses a **stack** to keep the asteroids that are still alive after resolving all collisions seen so far.

### Intuition

Only a right-moving asteroid followed later by a left-moving asteroid can collide. The stack stores unresolved asteroids, and when a new left-moving asteroid arrives, it repeatedly fights the right-moving asteroid on top of the stack until one side is destroyed or no collision is possible.

### Algorithm

Here's the step-by-step approach:

1. Traverse the asteroids from left to right.
2. If the current asteroid cannot collide with the top of the stack, push it.
3. Otherwise, while the top of the stack is moving right and the current asteroid is moving left, resolve collisions by comparing magnitudes and removing destroyed asteroids.
4. Append the current asteroid only if it survives all possible collisions, then return the stack.

### Code Breakdown

The `while` loop handles chain reactions, where one incoming asteroid may destroy multiple smaller asteroids already on the stack. The `else` attached to the loop runs only when the current asteroid survives the collision checks and should be kept.

### Why This Works

- **Only relevant collisions are checked:** A collision is possible exactly when the stack top moves right and the current asteroid moves left.
- **Correct elimination:** Comparing absolute sizes matches the rule that the smaller asteroid explodes, or both explode if equal.
- **Stable survivors:** After processing each asteroid, the stack represents the correct final state for the prefix seen so far.


## Complexity

- **Time**: `O(n)`
- **Space**: `O(n)`


## Similar Problems

- [Daily Temperatures](/TechInterviewHandbookDSA169/problems/daily-temperatures)
