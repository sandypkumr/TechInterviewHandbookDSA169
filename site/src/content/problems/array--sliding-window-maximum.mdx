---
title: "Sliding Window Maximum"
problemSlug: "sliding-window-maximum"
topic: "Array"
topicSlug: "array"
difficulty: "Hard"
leetcodeUrl: "https://leetcode.com/problems/sliding-window-maximum"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/array/sliding_window_maximum.py"
tags: ["Array", "Queue", "Sliding Window", "Monotonic Queue"]
patterns: ["Sliding Window", "Monotonic Deque"]
timeComplexity: "O(n)"
spaceComplexity: "O(k)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["minimum-window-substring", "find-k-closest-elements"]
---

## Problem Statement

You are given an array of integers `nums`, there is a sliding window of size `k` which is moving from the very left of the array to the very right. You can only see the `k` numbers in the window. Each time the sliding window moves right by one position.

Return _the max sliding window_.

 

**Example 1:**

```
Input: nums = [1,3,-1,-3,5,3,6,7], k = 3
Output: [3,3,5,5,6,7]
Explanation: 
Window position Max
--------------- -----
[1 3 -1] -3 5 3 6 7 3
 1 [3 -1 -3] 5 3 6 7 3
 1 3 [-1 -3 5] 3 6 7 5
 1 3 -1 [-3 5 3] 6 7 5
 1 3 -1 -3 [5 3 6] 7 6
 1 3 -1 -3 5 [3 6 7] 7
```

**Example 2:**

```
Input: nums = [1], k = 1
Output: [1]
```

 

**Constraints:**

- `1 <= nums.length <= 10^5`
- `-10^4 <= nums[i] <= 10^4`
- `1 <= k <= nums.length`

## Solution

```python
from collections import deque
from typing import List


class Solution:
    def maxSlidingWindow(self, nums: List[int], k: int) -> List[int]:
        if k == 1:
            return nums
        n = len(nums)
        res = []
        max_deque = deque()
        for i in range(n):
            # Remove elements not in the window
            if max_deque and max_deque[0] < i - k + 1:
                max_deque.popleft()
            # Remove smaller elements in the window
            while max_deque and nums[max_deque[-1]] <= nums[i]:
                max_deque.pop()
            max_deque.append(i)
            # Append the maximum element of the current window
            if i >= k - 1:
                res.append(nums[max_deque[0]])
        return res
```


## Complexity

- **Time**: `O(n)`
- **Space**: `O(k)`


## Similar Problems

- [Minimum Window Substring](/TechInterviewHandbookDSA169/problems/minimum-window-substring)
- [Find K Closest Elements](/TechInterviewHandbookDSA169/problems/find-k-closest-elements)
