---
title: "Add Binary"
problemSlug: "add-binary"
topic: "Binary"
topicSlug: "binary"
difficulty: "Easy"
leetcodeUrl: "https://leetcode.com/problems/add-binary"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/binary/add_binary.py"
tags: ["Math", "String", "Bit Manipulation", "Simulation"]
patterns: ["Bit Simulation", "String Addition"]
timeComplexity: "O(n)"
spaceComplexity: "O(n)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["single-number"]
---

## Problem Statement

Given two binary strings `a` and `b`, return _their sum as a binary string_.

 

**Example 1:**

```
Input: a = "11", b = "1"
Output: "100"
```
**Example 2:**

```
Input: a = "1010", b = "1011"
Output: "10101"
```

 

**Constraints:**

- `1 <= a.length, b.length <= 10^4`
- `a` and `b` consist only of `'0'` or `'1'` characters.
- Each string does not contain leading zeros except for the zero itself.

## Solution

```python
class Solution:
    def addBinary(self, a: str, b: str) -> str:
        return bin(int(a, 2) + int(b, 2))[2:]
```


## Explanation

### Approach: Convert from Binary -> Add -> Convert Back

This implementation uses Python's built-ins:
1. Parse both strings as base-2 integers
2. Add the two integers
3. Convert the result back to binary string

### Why This Matches the Problem

Binary addition is mathematically identical to integer addition once values are decoded. The built-ins handle carry logic internally.

### Step-by-Step Example

Input: `a = "1010"`, `b = "1011"`

- `int("1010", 2) = 10`
- `int("1011", 2) = 11`
- Sum = `21`
- `bin(21) = "0b10101"`
- Remove `"0b"` prefix -> `"10101"`

Output: `"10101"`

### Code Breakdown

```python
return bin(int(a, 2) + int(b, 2))[2:]
```

- `int(a, 2)`: parse `a` as base-2
- `+`: normal integer addition
- `bin(...)`: convert integer to binary format string
- `[2:]`: strip the `0b` prefix

### Complexity

- Parsing and formatting touch each digit, so O(n)
- Output string uses O(n) space


## Complexity

- **Time**: `O(n)`
- **Space**: `O(n)`


## Similar Problems

- [Single Number](/TechInterviewHandbookDSA169/problems/single-number)
