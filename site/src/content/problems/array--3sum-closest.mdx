---
title: "3Sum Closest"
problemSlug: "3sum-closest"
topic: "Array"
topicSlug: "array"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/3sum-closest"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/array/three_sum_closest.py"
tags: ["Array", "Two Pointers", "Sorting"]
patterns: ["Two Pointers", "Sorting"]
timeComplexity: "O(n²)"
spaceComplexity: "O(1)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["3sum"]
---

## Problem Statement

Given an integer array `nums` of length `n` and an integer `target`, find three integers at **distinct indices** in `nums` such that the sum is closest to `target`.

Return _the sum of the three integers_.

You may assume that each input would have exactly one solution.

 

**Example 1:**

```
Input: nums = [-1,2,1,-4], target = 1
Output: 2
Explanation: The sum that is closest to the target is 2. (-1 + 2 + 1 = 2).
```

**Example 2:**

```
Input: nums = [0,0,0], target = 1
Output: 0
Explanation: The sum that is closest to the target is 0. (0 + 0 + 0 = 0).
```

 

**Constraints:**

- `3 <= nums.length <= 500`
- `-1000 <= nums[i] <= 1000`
- `-10^4 <= target <= 10^4`

## Solution

```python
from typing import List


class Solution:
    def threeSumClosest(self, nums: List[int], target: int) -> int:
        nums.sort()
        closest_sum = float('inf')
        for i in range(len(nums) - 2):
            left, right = i + 1, len(nums) - 1
            while left < right:
                current_sum = nums[i] + nums[left] + nums[right]
                if abs(target - current_sum) < abs(target - closest_sum):
                    closest_sum = current_sum
                if current_sum < target:
                    left += 1
                else:
                    right -= 1
        return closest_sum
```


## Explanation

### Approach

It uses the **Two Pointers** technique to efficiently search through the data. The algorithm starts by **sorting** the input to enable more efficient processing. It employs a **Sliding Window** to track a contiguous subset of elements.

### Intuition

After sorting, we can use two pointers moving from opposite ends. This allows us to adjust our search based on whether the current sum is too large or too small, eliminating many unnecessary comparisons.

### Algorithm

1. **Sort the input**: Arrange elements in ascending order
2. **Iterate through elements**: Process each element systematically
3. **Move pointers**: Adjust left/right pointers based on current sum
4. **Return result**: Output the computed answer

### Why This Works

- **Two pointers optimization**: Eliminates need for nested loops by intelligently moving pointers
- **Sorting benefit**: Enables binary search, two pointers, and easy duplicate handling
- **Correctness**: The algorithm handles all edge cases and produces the expected output
- **Efficiency**: Optimized for both time and space constraints


## Complexity

- **Time**: `O(n²)`
- **Space**: `O(1)`


## Similar Problems

- [3Sum](/TechInterviewHandbookDSA169/problems/3sum)
