---
title: "Implement Trie (Prefix Tree)"
problemSlug: "implement-trie-prefix-tree"
topic: "Trie"
topicSlug: "trie"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/implement-trie-prefix-tree"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/trie/implement_trie_prefix_tree.py"
tags: ["Hash Table", "String", "Design", "Trie"]
patterns: ["Trie", "HashMap Children"]
timeComplexity: "O(M) per op"
spaceComplexity: "O(M×N)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["design-add-and-search-words-data-structure", "word-search-ii"]
---

## Problem Statement

A **trie** (pronounced as "try") or **prefix tree** is a tree data structure used to efficiently store and retrieve keys in a dataset of strings. There are various applications of this data structure, such as autocomplete and spellchecker.

Implement the Trie class:

- `Trie()` Initializes the trie object.
- `void insert(String word)` Inserts the string `word` into the trie.
- `boolean search(String word)` Returns `true` if the string `word` is in the trie (i.e., was inserted before), and `false` otherwise.
- `boolean startsWith(String prefix)` Returns `true` if there is a previously inserted string `word` that has the prefix `prefix`, and `false` otherwise.

 

**Example 1:**

```
Input
["Trie", "insert", "search", "search", "startsWith", "insert", "search"]
[[], ["apple"], ["apple"], ["app"], ["app"], ["app"], ["app"]]
Output
[null, null, true, false, true, null, true]

Explanation
Trie trie = new Trie();
trie.insert("apple");
trie.search("apple"); // return True
trie.search("app"); // return False
trie.startsWith("app"); // return True
trie.insert("app");
trie.search("app"); // return True
```

 

**Constraints:**

- `1 <= word.length, prefix.length <= 2000`
- `word` and `prefix` consist only of lowercase English letters.
- At most `3 * 10^4` calls **in total** will be made to `insert`, `search`, and `startsWith`.

## Solution

```python
class Trie:
    def __init__(self):
        self.root = {}
        self.end_of_word = "#"

    def insert(self, word: str) -> None:
        node = self.root
        for char in word:
            node = node.setdefault(char, {})
        node[self.end_of_word] = self.end_of_word

    def search(self, word: str) -> bool:
        node = self.root
        for char in word:
            if char not in node:
                return False
            node = node[char]
        return self.end_of_word in node

    def startsWith(self, prefix: str) -> bool:
        node = self.root
        for char in prefix:
            if char not in node:
                return False
            node = node[char]
        return True
```


## Explanation

### Approach: Trie (Prefix Tree) with Hash Map

This solution implements a **Trie** data structure, where each node contains a hash map of children. This efficiently stores and searches strings with common prefixes.

### Intuition

**Problem:** We need to store many strings and quickly:
- Check if a word exists
- Check if a prefix exists
- Share common path for common prefixes

**The Trie Solution:** Build a tree where:
- Each node represents a character
- Each path from root to a marked node represents a complete word
- Nodes with common prefixes share the same path

**Benefit:** All operations share common prefixes, saving space and time.

### Step-by-Step Walkthrough

Building a Trie with words ["apple", "app"]:

```
       root
       /
      a
      |
      p
      |
      p
      |  (mark as end-of-word)
      l
      |
      e  (mark as end-of-word)

- search("apple"): Follow a→p→p→l→e, check if marked. YES
- search("app"): Follow a→p→p, check if marked. YES
- search("ap"): Follow a→p, check if marked. NO
- startsWith("app"): Follow a→p→p, exists. YES
```

### Code Breakdown

```python
def __init__(self):
    self.root = {}                    # Root node as empty dict
    self.end_of_word = "#"            # Marker for word end
```
**Structure:** Each node is a dict mapping characters to child nodes.

```python
def insert(self, word: str) -> None:
    node = self.root
    for char in word:
        node = node.setdefault(char, {})    # Navigate/create path
    node[self.end_of_word] = True           # Mark end of word
```
**Insert:** Navigate through characters, creating nodes as needed. Mark the final node as a word end.

```python
def search(self, word: str) -> bool:
    node = self.root
    for char in word:
        if char not in node:
            return False                    # Character not in path
        node = node[char]
    return self.end_of_word in node         # Must be marked as word end
```
**Search:** Follow the exact path for the word. Return true only if the path ends with a marked node.

```python
def startsWith(self, prefix: str) -> bool:
    node = self.root
    for char in prefix:
        if char not in node:
            return False                    # Character not in path
        node = node[char]
    return True                             # Path exists (no end mark needed)
```
**StartsWith:** Similar to search, but we don't require an end-of-word marker.

### Why This Works

- **Prefix Sharing:** Common prefixes share nodes, saving space
- **O(M) Operations:** M = length of word; not dependent on number of words
- **Efficient Prefix Matching:** Easy to find all words with a given prefix
- **Hash Map Children:** Using dict for children provides O(1) character lookup


## Complexity

- **Time**: `O(M) per op`
- **Space**: `O(M×N)`


## Similar Problems

- [Design Add And Search Words Data Structure](/TechInterviewHandbookDSA169/problems/design-add-and-search-words-data-structure)
- [Word Search Ii](/TechInterviewHandbookDSA169/problems/word-search-ii)
