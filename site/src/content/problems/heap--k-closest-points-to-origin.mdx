---
title: "K Closest Points to Origin"
problemSlug: "k-closest-points-to-origin"
topic: "Heap"
topicSlug: "heap"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/k-closest-points-to-origin"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/heap/k_closest_points_to_origin.py"
tags: ["Array", "Math", "Divide and Conquer", "Geometry", "Sorting", "Heap", "Quickselect"]
patterns: ["Max Heap", "Quickselect"]
timeComplexity: "O(n log k)"
spaceComplexity: "O(k)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["kth-largest-element-in-an-array", "top-k-frequent-words"]
---

## Problem Statement

Given an array of `points` where `points[i] = [x_i, y_i]` represents a point on the **X-Y** plane and an integer `k`, return the `k` closest points to the origin `(0, 0)`.

The distance between two points on the **X-Y** plane is the Euclidean distance (i.e., `&radic;(x_1 - x_2)^2 + (y_1 - y_2)^2`).

You may return the answer in **any order**. The answer is **guaranteed** to be **unique** (except for the order that it is in).

 

**Example 1:**

```
Input: points = [[1,3],[-2,2]], k = 1
Output: [[-2,2]]
Explanation:
The distance between (1, 3) and the origin is sqrt(10).
The distance between (-2, 2) and the origin is sqrt(8).
Since sqrt(8) < sqrt(10), (-2, 2) is closer to the origin.
We only want the closest k = 1 points from the origin, so the answer is just [[-2,2]].
```

**Example 2:**

```
Input: points = [[3,3],[5,-1],[-2,4]], k = 2
Output: [[3,3],[-2,4]]
Explanation: The answer [[-2,4],[3,3]] would also be accepted.
```

 

**Constraints:**

- `1 <= k <= points.length <= 10^4`
- `-10^4 <= x_i, y_i <= 10^4`

## Solution

```python
from typing import List


class Solution:
    def kClosest(self, points: List[List[int]], k: int) -> List[List[int]]:
        def distance(point):
            return point[0] ** 2 + point[1] ** 2
        return sorted(points, key=distance)[:k]
```


## Explanation

### Approach

This solution uses **Depth-First Search (DFS)** to explore all possible paths. The algorithm starts by **sorting** the input to enable more efficient processing.

### Intuition

The solution breaks down the problem into manageable steps, processing elements systematically to build the final answer.

### Algorithm

1. **Sort the input**: Arrange elements in ascending order
2. **Return result**: Output the computed answer

### Why This Works

- **Sorting benefit**: Enables binary search, two pointers, and easy duplicate handling
- **Correctness**: The algorithm handles all edge cases and produces the expected output
- **Efficiency**: Optimized for both time and space constraints


## Complexity

- **Time**: `O(n log k)`
- **Space**: `O(k)`


## Similar Problems

- [Kth Largest Element In An Array](/TechInterviewHandbookDSA169/problems/kth-largest-element-in-an-array)
- [Top K Frequent Words](/TechInterviewHandbookDSA169/problems/top-k-frequent-words)
