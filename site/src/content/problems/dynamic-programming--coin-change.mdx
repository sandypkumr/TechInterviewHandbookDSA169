---
title: "Coin Change"
problemSlug: "coin-change"
topic: "Dynamic Programming"
topicSlug: "dynamic-programming"
difficulty: "Medium"
leetcodeUrl: "https://leetcode.com/problems/coin-change"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookDSA169/blob/master/src/dynamic_programming/coin_change.py"
tags: ["Array", "Dynamic Programming", "Breadth-First Search"]
patterns: ["Dynamic Programming", "Unbounded Knapsack", "Bottom-Up DP"]
timeComplexity: "O(amount × n)"
spaceComplexity: "O(amount)"
hasProblemStatement: true
hasConstraints: true
hasExamples: true
isPremium: false
similarProblems: ["combination-sum-iv", "partition-equal-subset-sum"]
---

## Problem Statement

You are given an integer array `coins` representing coins of different denominations and an integer `amount` representing a total amount of money.

Return _the fewest number of coins that you need to make up that amount_. If that amount of money cannot be made up by any combination of the coins, return `-1`.

You may assume that you have an infinite number of each kind of coin.

 

**Example 1:**

```
Input: coins = [1,2,5], amount = 11
Output: 3
Explanation: 11 = 5 + 5 + 1
```

**Example 2:**

```
Input: coins = [2], amount = 3
Output: -1
```

**Example 3:**

```
Input: coins = [1], amount = 0
Output: 0
```

 

**Constraints:**

- `1 <= coins.length <= 12`
- `1 <= coins[i] <= 2^31 - 1`
- `0 <= amount <= 10^4`

## Solution

```python
from typing import List


class Solution:
    def coinChange(self, coins: List[int], amount: int) -> int:
        dp = [float("inf")] * (amount + 1)
        dp[0] = 0
        for i in range(1, amount + 1):
            for coin in coins:
                if i >= coin:
                    dp[i] = min(dp[i], dp[i - coin] + 1)
        return dp[amount] if dp[amount] != float("inf") else -1
```


## Complexity

- **Time**: `O(amount × n)`
- **Space**: `O(amount)`


## Similar Problems

- [Combination Sum Iv](/TechInterviewHandbookDSA169/problems/combination-sum-iv)
- [Partition Equal Subset Sum](/TechInterviewHandbookDSA169/problems/partition-equal-subset-sum)
